# 本轮测评方案核验记录

> ⚠️ **本文是改动前的历史证据**（记录的是「方案阶段」观察到的缺陷与基线数字），
> 保留原样不修改。本轮实施后的复现、修复与验收结果见同目录
> [实施核验记录.md](实施核验记录.md)。

日期：2026-09-15。范围：当前代码、现有内容、合成答卷、本机运行页面；未判断任何真人的类型。

## 1. 工程检查

从 frontend 目录直接执行（避开 npm pretest 的生成写入）：

```powershell
node node_modules/vitest/vitest.mjs run src/domain/scoring.spec.ts src/domain/clarity.spec.ts src/domain/answers.spec.ts
```

结果：3个文件通过，50个测试通过；scoring 27、clarity 9、answers 14。说明被覆盖的数值及答案处理行为满足当前测试，不能证明中文测量有效。

## 2. 独立输入探针

```powershell
node docs/2026-09-15/assessment-evidence/score-probe.cjs
```

[脚本](score-probe.cjs)只读取现有TypeScript和YAML，在内存转译并运行当前计分函数；不替换应用源码、不写业务数据。[输出](score-probe.json)包含7组合成答卷、固定期望检查、源文件SHA-256与时间。

结果：7/7符合原公式。全中立与每维正负端点抵消都返回24/24/24/24、ISFJ；两者中立题数分别32与0。全中立仅Q3改2即返回ESFJ。不能把这些工程样例作为人群统计分布。

## 3. 本机浏览器实际复现

- 当前前端地址：`http://127.0.0.1:5173/#/`；页面为已改版应用。现有8080内容服务处于监听状态，未在本轮重启或重新打包。
- 使用新建后台浏览器标签，从“开始测试”进入，逐题点击32次“一半一半”，逐题下一题，最后点击“查看结果”。未通过种子/改存储注入答案。
- 首页初始无历史记录。本次写入仅为该浏览器中的合成测试答卷，没有改动用户工程内容。
- 结果实际出现“你的四个维度都接近均衡”及“兼容写法：ISFJ”。
- 同一页面仍出现“主导侧（I·内向）”及ISFJ正文，例如日常表现“每位同事不吃什么、哪个时段不方便，你都记得”，并给出对应优势和行动建议。
- 点击“生成分享卡片”，实际预览为大字ISFJ、名称“细节照顾者”，下方才提示四维均衡。已通过本轮截图目视核验；未触发系统分享、复制、下载或任何外部发送。
- CUA当前不支持页面内容export，因此未声称保存了完整DOM导出或新截图文件；上述为本轮直接观察记录，截图可见本任务工具记录。

## 4. 源码对应位置

- `frontend/src/domain/scoring.ts`：`toDimensionResult`对任何分数都给dominant；`scoreQuestionnaire`始终拼typeCode。
- `frontend/src/components/DimensionBar.vue`：均衡描述仍拼接dominantLabel和dominant字母。
- `frontend/src/views/ResultView.vue`：allBalanced仅调整部分展示，ensureReport仍按typeCode读取profile；daily/strengths/actions仍由profile控制。
- `frontend/src/utils/shareImage.ts`：封面绘制result.typeCode与profile.nameCn；shareText和文件名也使用typeCode。
- `frontend/src/domain/answers.ts`：五档与数字有效性，没有无法判断状态。
- `backend/src/main/resources/content/questionnaire-quick.yml`：32题目前措辞，供审校表逐项对照。

源文件具体内容以同目录JSON中的哈希对应版本为准；接手方需重新核验，不能把本轮观察当作未来版本仍存在的问题。

## 5. 界限

本轮没有运行全项目测试/真实手机测试，没有做心理学样本研究，没有证明候选译文等效，也没有修改或发布业务实现。历史verification/REPORT.md中的156条断言是历史资料，未作为本轮通过数量。

## 6. 文档交付检查

- 三份方案及本记录的相对文件链接可解析，Markdown代码围栏成对。
- 审校表题号1–32齐全；题目维度和正反号与当前维护源对应。
- 生成内容`--check`再次通过；探针记录的6个业务源文件哈希与交付前相同。
- 这些检查只证明方案结构、索引与本轮源码证据一致，不证明拟实施功能已经完成。
