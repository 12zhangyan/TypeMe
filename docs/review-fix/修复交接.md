# TypeMe 快速版 32 题 Review 修复交接

> 日期：本次会话
> 上游聚合：`docs/review-fix/审查聚合.md`（含完整证据与 reviewer 原文别名）
> 状态：**待执行**
> 执行方式：**2 个执行 Agent 并行，文件所有权互不重叠**

---

## 一、Review 结果汇总

### Critical（必须修）— 1 条

| ID | 来源 | 文件/位置 | 问题 | 影响 | 修复建议 | 验证方式 |
|---|---|---|---|---|---|---|
| **CR-1** | R4-1 + R4-4 + R1-5 + R4-7 | `frontend/src/stores/quiz.ts:104-110`、`frontend/src/views/ResultView.vue:41-49,104-112`、`frontend/src/api/client.ts:89-117` | `isComplete` 只判 `typeof number`，与计分器的「值 ∈ 1..5」口径不一致。越界值时 `isComplete=true` → 计分抛错 → `ResultView` catch 后 `router.replace('/quiz')` → 答题页显示 disabled 的「还差 N 题」→ **来回弹，永远看不到结果**。另有两条同源入口：`isValidQuestionnaire` 不校验 constants 四键（`constants:{}` 通过）；`restore()` 不校验值（`answers:{"1":"3"}` 使该题永久无法答完） | 核心流程不可用，用户唯一出路是「重新测一次」 | ① `isComplete` 改用与计分一致的口径（值 ∈ 1..5）；② `isValidQuestionnaire` 补 constants 四键存在 + 有限数值 + `questions.length === questionCount` + id 唯一；③ `restore()` 做 `Number()` + 值域过滤；④ `ResultView` 区分「未答完」（replace）与「结构性错误」（**渲染错误态 + 重测入口，不 replace**） | 补三条单测：越界值 / 坏 constants / 字符串作答 —— 断言**能渲染结果或错误态，且不发生 replace** |

### Important（修完再继续）— 8 条

| ID | 来源 | 归属 | 文件/位置 | 问题 | 修复建议 | 验证方式 |
|---|---|---|---|---|---|---|
| **IM-1** | R4-2 + R2-1 + R5-1/2/3 + R1-4 + R3-09 | **R-A** | `scripts/gen-fallback-content.mjs`（不存在）、`frontend/src/content/{fallback,typeProfiles}.ts`、`backend/src/main/resources/content/*.yml` | 前端三份内容（题库/16 型文案/方法页）是**独立手写的第二份真相**：题库 30/32 题措辞不同、**16 型文案全部字段不同（nameCn 0/16 相同）**、方法页 4 节 vs 6 节且 `contentVersion` 不一致。守卫为空 | ① 写 `scripts/gen-fallback-content.mjs`：从后端 YAML **单向生成** `fallback.ts` / `typeProfiles.ts` / 方法页副本，纳入 `npm run build`（prebuild）；② **按 BK-1 裁决删除前端原创名**，统一用后端 `types.yml` 的通行译名；③ 加跨源一致性测试（逐字段含文本） | 故意改后端 YAML 一处 → 测试必须红；`npm run build` 后两份内容逐字段相等 |
| **IM-3** | R3-01 | **R-A** | `backend/pom.xml:34-59`、缺 `backend/src/main/resources/static/` | 构建未把 `frontend/dist` 复制进产物，真实形态下 `/`、`/quiz`、`/result`、`/about` 全 404。测试全绿是因为 `src/test/resources/static/index.html` **夹具盖住了问题** | 加 `maven-resources-plugin`（或 frontend-maven-plugin）把 `frontend/dist` 复制进 `target/classes/static`；补一条**不带夹具**的集成测试断言 `/` 返回真实 index.html | 去掉夹具后 `/` 仍 200 且返回真实产物 |
| **IM-4** | R1-1 | **R-B** | `frontend/src/domain/scoring.ts:132-135,161,162` | `span`/`midpoint`「由题库推导」这一性质**无回归防线**：变异实测硬编码 midpoint → 17/17 绿、硬编码 span → 17/17 绿、分母写死 16 → 17/17 绿 | 加断言 `buildDimensionScales({EI:31,...}).EI.span === 17`；加 midpoint=20 题库的极性翻转测试 | 上述三种变异必须转红 |
| **IM-5** | R2-3 + R5-4 | **R-A** | `backend/src/main/resources/content/method.yml:18` | 许可声明写「没有改写、增删」，与同页 `AttributionBlock.vue:52`「进行了中文本地化改写」及 `fallback.ts:174` **正面矛盾**；事实是措辞已大幅重写 | 改为：「本站对英文题项做了中文本地化改写，**左右两端的极向与计分符号未改动**」——后半句确实成立 | 三处表述一致 |
| **IM-6** | R6-1 + R6-2 + R6-7 | **R-B** | 全部 `frontend/src/**/*.vue`、`frontend/src/style.css`、`tailwind.config.js` | **PC 桌面布局完全缺失**：947 个 class token 中带断点前缀仅 16 个（1.7%）且全是 `sm:`；产物 CSS 无任何 ≥768px 的 `@media`；实测 1280/1920 下 `main` 都是 608px，`/quiz` @1920 题卡占视口宽 30%、下方 620px 全空 | 引入 `md:`/`lg:` 层级：`md:` 起放宽 `main` 或改两栏；`lg:` 起答题页一屏 2–3 题或加左侧进度栏；`md:` 起提升正文与辅助字号、恢复桌面 CTA 尺寸 | 在 1280/1920 两个视口断言题卡占视口宽度比例下限；补桌面断点断言（现有 `views.spec.ts` **无任何断点断言**） |
| **IM-7** | R6-3 | **R-B** | `frontend/src/utils/shareImage.ts:307-343`、`ResultView.vue:128-138,311-337,340-362` | ① 全前端 `navigator.share`/`canShare` **grep 零命中**，新 F-04 未实现；② **下载成功时零可见反馈**（实测 `textChanged:false`、无 toast）；③ 文案仍是微信专属（「微信内置浏览器里没法直接唤起…」），PC 上语义不通 | ① 增加 `navigator.canShare({files})` 检测 → `navigator.share`，失败降级 `<a download>`；② `downloadShareCard` 返回结果，**成功与失败都给可见反馈**；③ 文案按环境中性化 | mock 成功与静默失败两条路径，断言均有可见反馈 |
| **IM-8** | R6-5 + R6-4 | **R-B** | `frontend/src/components/LikertScale.vue`、`views/QuizView.vue:93-105` | 键盘硬伤（实测）：新题未作答时 Enter/ArrowRight **无反应且无反馈**；数字键选完后焦点**始终在 BODY**，自动跳题后焦点丢失；`role=radiogroup` 却无 roving tabindex、无组内 keydown。另：选中即 240ms 自动跳题为移动端推导，桌面无确认/下一题 | ① 补 roving tabindex + 组内方向键 + Space/Enter 选择；② 未作答时给反馈而非静默；③ 跳题后焦点移回题卡标题；④ `md:` 起关闭/延长自动跳题并提供「下一题」 | 补键盘路径用例（现存无任何键盘断言） |
| **IM-2** | R4-3 | **R-B** | （已因去微信化降为 MI，其"无反馈"部分并入 IM-7） | — | — | — |

### Minor（12 组，不阻塞但建议处理）

| ID | 归属 | 内容 |
|---|---|---|
| MI-1 | R-A | 校验未覆盖 `title` / `estimatedMinutes`（999 → 落地页显示「约 999 分钟」） |
| MI-2 | R-A | **题目数组顺序既不在校验也不在哈希覆盖内**——Q1↔Q2 换位校验通过**且哈希不变**，唯一连测试都拦不住的类别。修：规范串按 id 排序后拼接，或断言 id 单调递增 |
| MI-3 | R-A | 反序列化过宽：`direction: 1.9`→1、`questionCount:"32"`→32 静默通过；types/method 侧未知字段被静默忽略。修：关掉 `ACCEPT_FLOAT_AS_INT` 与字符串强转，统一严格模式 |
| MI-4 | R-A | `types.yml` 缺失只 WARN、`health` 仍 UP、`/types/*` 全 404、无监控信号。修：加 HealthIndicator 或列为硬依赖 |
| MI-5 | R-B | `client.ts:185-196` 未知版本降级后给 32 题贴上 `standard` 标签。修：未知版本直接报错 |
| MI-6 | R-B | 显示层硬编码「中点 24，区间 8–40」；极点字母映射分散 4 处各自硬编码，只改 UI 可让测试全绿而倾向条与四字母相反。修：收敛为 domain 唯一导出 + 断言 |
| MI-7 | R-A | 题库翻译质量批次：fallback Q16/Q22/Q29 语义漂移与褒贬、Q10/Q18 构念窄化、后端 Q6 语义偏移、翻译腔清单。**注意：改 `questionnaire-quick.yml` 即可（fallback 由生成脚本产出）** |
| MI-8 | R-B | 缺答时无「跳到未答题」入口；`loadProfile` 无 catch → unhandled rejection；计分只对「0 个维度」抛错 |
| MI-9 | R-B | canvas 无 JPEG/降分辨率兜底；剪贴板降级文案在移动端选不中文本；**首屏不提示正在使用内置副本** |
| MI-10 | R-B | 落地页**硬编码署名、从不请求 `/api/v1/meta`**（许可变更时 BY 义务会静默过期）；关于页重复来源提示块；fallback 用「68%」作反例 |
| MI-11 | R-A / R-B | `WebConfig` javadoc 与行为不一致（R-A）；`frontend/README.md:54` 已过时（R-B） |
| MI-12 | R-A | **16 型文案跨类型自我重复**：ENTJ 与 ESTJ 的 `dimensions.TF` **有 20 字整句逐字相同**；ENFP 与 ESFP 的 `growth` 10 字连续相同；模板框式被 2–3 型共用；6 字以上跨类型复用共 58 处。修：跨类型去重 |

### Rejected（不采纳）

| ID | 原建议 | 拒绝原因 |
|---|---|---|
| RJ-1 | 要求修正 OEJTS 的 Q19/Q21 褒贬问题 | **前提错误**：那两题属 IPIP 中文译本缺陷，OEJTS 无此题。属派单方（主 Agent）的输入错误，非代码缺陷 |

### Blockers（已全部裁决，无阻塞）

| ID | 裁决结果 |
|---|---|
| BK-1 | ✅ **用后端 `types.yml` 的通行译名**；前端原创名删除；`需求文档 §6.2` 红线已同步修订；正文原创性已由 R7 验证（与 16Personalities 中文全 16 页比对，最长公共子串 5 字） |
| BK-2 | ✅ **Q9 改为中性对照**：「杂乱无章 / 井井有条」→「**随性，有点乱 / 有条理，按规矩放**」。**必须保持左=chaotic 端、右=organized 端，`direction: -1` 不变**（写反会让符号反转）。并在方法页说明此题与原量表措辞有差异 |
| BK-3 | ✅ **补「致谢与参考文献」小节**（第 5 节），同步更新 `ContentService.REQUIRED_METHOD_SECTIONS` |

---

## 二、修复边界

### 文件所有权（**严格，两个 Agent 不得越界**）

| Agent | 独占 | 禁止触碰 |
|---|---|---|
| **R-A** 后端 + 内容 + 生成脚本 | `backend/**`、`scripts/**`、`frontend/src/content/**` | 其余 `frontend/**` |
| **R-B** 前端应用层 | `frontend/**` **除** `src/content/` | `backend/**`、`scripts/**`、`frontend/src/content/**` |

> ⚠️ **R-A 会重新生成 `frontend/src/content/typeProfiles.ts`，16 型名字将从原创名变为通行译名（调停者/建筑师…）**。
> **R-B 的测试不得断言具体 `nameCn` 字面量**，否则会与 R-A 的产物冲突。需要断言类型名时，用「非空」或从内容模块读取。

### 允许
- 只修改 accepted findings 涉及的文件与必要测试
- 补测试、补注释、更新受影响的文档
- 运行 `mvn test` / `npx vitest run` / `npx vue-tsc --noEmit` / `npm run build`

### 禁止
- 不要改**冻结契约**（`docs/任务拆解.md` §1.1 的常量表、§1.4 API 端点与字段名、§1.5 TS 类型字段名）
- 不要引入数据库、持久化、账号、付费、AI
- 不要 `git add` / `commit` / `push`
- 不要重构无关代码
- **不要输出人群百分位**；不要引入红线词（最准/官方/权威/诊断）
- 不要改动 OEJTS 题目的**左右端顺序**与 `direction` 符号（除 BK-2 指定的 Q9 措辞外）

### 环境
JDK 21：`D:\develop\jdk-21`；Maven：`D:\develop\maven-public\apache-maven-3.9.16`。前端自带 `node_modules`。

---

## 三、修复 Todo

### R-A（后端 + 内容 + 生成脚本）
- [ ] CR-1 无（CR-1 归 R-B）
- [ ] **IM-1**：写 `scripts/gen-fallback-content.mjs`，从后端 YAML 单向生成前端三份内容；纳入 `prebuild`；加跨源一致性测试
- [ ] **IM-3**：`pom.xml` 加资源复制，把 `frontend/dist` 打进 `target/classes/static`；补不带夹具的 `/` 断言
- [ ] **IM-5**：修 `method.yml:18` 许可措辞
- [ ] **BK-2**：改 `questionnaire-quick.yml` 的 Q9 两端措辞（**保持左右与符号不变**）+ 方法页说明
- [ ] **BK-3**：`method.yml` 加第 5 节「致谢与参考文献」+ 更新 `REQUIRED_METHOD_SECTIONS`
- [ ] **MI-12**：`types.yml` 跨类型去重（优先 ENTJ/ESTJ 那条 20 字逐字重复）
- [ ] MI-1 / MI-2 / MI-3 / MI-4 / MI-7 / MI-11(后端部分)
- [ ] 验证：`mvn test` 全绿；坏内容仍能使启动失败（回归）；生成脚本一致性测试绿

### R-B（前端应用层）
- [ ] **CR-1**：统一有效作答口径 + `isValidQuestionnaire` 补校验 + `restore()` 过滤 + `ResultView` 错误态；补三条单测
- [ ] **IM-4**：`span`/`midpoint` 回归断言 + midpoint=20 极性测试
- [ ] **IM-6**：引入 `md:`/`lg:` 断点，完成 PC 桌面布局 + 桌面字号
- [ ] **IM-7**：`navigator.share` 优先 + 成功/失败可见反馈 + 文案去微信化
- [ ] **IM-8**：radiogroup 键盘语义 + 未作答反馈 + 焦点管理 + 桌面「下一题」
- [ ] MI-5 / MI-6 / MI-8 / MI-9 / MI-10 / MI-11(前端部分)
- [ ] 验证：`npx vitest run` 全绿（含新增断言）；`npx vue-tsc --noEmit` 干净；`npm run build` 成功

### 共同
- [ ] 两边都跑一次完整验证，并在返回消息中给出**真实退出码与用例数**，不得编造
- [ ] 在各自返回摘要中说明：**未能验证的部分**（尤其需真机的项）

---

## 四、下一动作

```text
使用 yan-code-review skill，mode=repair，读取 docs/review-fix/修复交接.md；
按 canonical finding ID 修复各自归属的条目，并回填实际验证结果。
```

修复完成后由主 Agent 派 reviewer 做**聚焦复审**：只复审被修改路径 + 相邻风险 + 测试有效性，不重跑全量。

---

## 五、残留风险（修复后仍需人工处理）

1. **真机实测**（自动化测不出）：PC 键鼠 + 手机浏览器各自的下载/分享落地行为、移动端 `100vh` 抖动、真实键盘空格激活
2. ~~3 位朋友试读 16 型文案~~ → **【裁决】不做上线前试读**。改为上线后由真实使用自然验证（朋友看到自己的结果页就是试读）。
   - **仍保留**的机器可验标准：跨类型 `resonance` Jaccard < 0.7、同一物象不跨 ≥3 型、无跨 3 型以上复用的模板句式
   - ⚠️ **残留风险**：机器检查证明不了"读起来真的像一个人"。若上线后发现某型普遍被认为"不像"，**只重写该型**
3. **部署步骤**（本次范围外）：把 `frontend/dist` 复制进后端产物后，能否在 `IP:8080` 真实跑通
