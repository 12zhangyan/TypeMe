# jung48 题库逐题审校表（typeme-jung48-zh-v1 · 草案）

对应内容文件：`docs\2026-09-16\implementation\content\jung48-questions.yml`（同一提交内容，本表由该文件当前内容生成，未手工转写）。

- 契约依据：`_contracts/01-新测契约-v1.md` §1.1 极点正负、§2.1 题号、§2.2 主测轮转、§2.3 左右平衡、§2.4 发布期校验清单。
- 极点约定（**每维第一个字母为负极**）：EI→负极 `I` / 正极 `E`；SN→`S` / `N`；TF→`T` / `F`；JP→`J` / `P`。
- `direction` 由契约 §4 推出：`question.rightPole == 正极` 时 `direction = +1`，否则 `direction = -1`；单题贡献 `c = direction × (rating − 3)`。
- 内容状态：`draft_review_pending`。**本表不构成任何信效度结论。**

## 1. 逐题审校

| 题号 | 阶段 | 维度 | 场景(facet) | 左端文字 | 左极点 | 右端文字 | 右极点 | 该题在测什么 | 方向说明（direction 由哪端为正极推出） | 可能的歧义/误读风险 | 修改原因/创作说明 结论 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| EI-01 | 主测 | EI | 活动之后的精力（精力恢复） | 留点时间自己安静待着 | `I` | 再找个人聊聊刚发生的事 | `E` | 互动后精力流向：独处回血还是与人复盘 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | “聊聊刚发生的事”可能被读成“喜欢分享”，与 E 的社交意愿混淆 | 初稿；把两侧都写成被动可接受的休息方式，避免暗示哪一侧更热情 保留 |
| SN-01 | 主测 | SN | 开始了解新东西（学新东西的入口） | 一个能照着做的具体例子 | `S` | 它整体怎么运作、各部分怎么连 | `N` | 接触新领域的入口：先取例子还是先取框架 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | “整体怎么运作”可能被当成“能力强的人”才会关心，需真人确认不产生能力暗示 | 初稿；两侧都描述“想先拿到什么”，不评价深浅 保留 |
| TF-01 | 主测 | TF | 两个方案影响不同人（做取舍的优先项） | 谁会受到什么影响，哪些需要被照顾 | `F` | 用同一套标准比较，看前后是否一致 | `T` | 两方案影响不同人时的取舍依据 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 用户可能认为“兼顾人与标准”才对，两侧都存在被当成“正确答案”的风险 | 按契约左右平衡要求做过镜像互换；这是全卷 T/F 风险最高的题之一 保留 |
| JP-01 | 主测 | JP | 自己安排的一天（安排周末/任务的方式） | 先把大致顺序和时间定下来 | `J` | 先留出空间，到那时再决定 | `P` | 自主安排一天的默认形态 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | “先定时间”可能被读成“自律”，需确认没有勤奋/懒惰的暗示 | 初稿；把 P 侧写成“留出空间”，不写成“没有计划” 保留 |
| EI-02 | 主测 | EI | 想法还没理清时（思考方式） | 先说出口，一边说一边理顺 | `E` | 自己先想一遍，再拿出来说 | `I` | 想法未成形时的外显习惯 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | 可能被理解为“说话前动不动脑子”，存在评价性误读风险 | 镜像为 E 在左以满足左右平衡；措辞刻意去掉“随口说”等贬义 保留 |
| SN-02 | 主测 | SN | 说明一件刚发生的事（描述一件事的方式） | 按顺序把经过和原话说清楚 | `S` | 先说它说明什么、和什么有关 | `N` | 讲述一件事的默认顺序 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | “先说结论”可能被当成表达能力更强，需真人确认 | 初稿；两侧都当成正常的讲法，不分高下 保留 |
| TF-02 | 主测 | TF | 朋友来找你商量（看人做事） | 先弄清他真正在意什么、为难在哪 | `F` | 帮他看清哪个选择更站得住 | `T` | 朋友来商量时先进入哪一层 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 容易被读成“会不会安慰人”，可能引入人际能力暗示 | 初稿；把 T 侧写成“帮他看清”，不写成“讲道理压人” 保留 |
| JP-02 | 主测 | JP | 没有期限的任务（决定与保留选项） | 先定下一套做法，往下推进 | `P` | 同时留着几套做法，看情况再说 | `J` | 无期限任务的推进方式 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “保留几套做法”可能被读成“拖延不决” | 初稿；强调“不急且有几种做法都行”，把现实限制排除在外 保留 |
| EI-03 | 主测 | EI | 到了不太熟的场合（社交场合的选择） | 先走动，和几个人聊上几句 | `E` | 先待在旁边看一会儿，等有人来搭话 | `I` | 半陌生场合的进入方式 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | 极易被读成胆量或社交能力；本卷已在措辞上避免“敢不敢” | 初稿；此题为 E/I 高风险题，建议列入首批试测 建议改写 |
| SN-03 | 主测 | SN | 读一份介绍材料（对细节与整体的注意力） | 它讲到的实际情况和具体做法 | `S` | 它背后的思路和整体方向 | `N` | 读介绍材料时抓到什么 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | “整体方向”可能被读成更聪明的读法，需确认不产生智商暗示 | 经左右镜像互换以修正 SN 维 6:6 平衡；两侧均为正常阅读偏好 保留 |
| TF-03 | 主测 | TF | 规定遇上特殊情况（面对规则与例外） | 先看这个人现在具体难在哪 | `F` | 先问这条规定为什么这样定 | `T` | 规定遇到特殊情况时的第一问 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 可能被读成“通融=好人 / 守规=刻板”，需真人确认无道德排序 | 初稿；两侧都是合理的处理顺序，不暗示哪个更高尚 保留 |
| JP-03 | 主测 | JP | 原定安排被改动（行程变化的适应） | 很快接受改动，重新安排就好 | `P` | 先照原来的计划走，再想怎么补回来 | `J` | 原定安排被打乱时的反应 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “很快接受改动”可能被读成“没主见” | 初稿；把 J 侧写成“照原计划走”，不写成“固执” 保留 |
| EI-04 | 主测 | EI | 话头被人接过去（被打断后的反应） | 先听他说完，再接着讲自己想讲的 | `I` | 顺势聊下去，想到哪说到哪 | `E` | 话头被接走之后的走向 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | “接话”与“回到自己想讲的”都像礼貌问题，与 E/I 关联偏弱，可能测成沟通风格 | 初稿；此题与 E/I 的构念距离最远，列为优先试测并准备改写或替换 建议改写 |
| SN-04 | 主测 | SN | 学一样新技能（学新东西的入口） | 先跟着做几遍，把动作学会 | `S` | 先弄懂它为什么管用 | `N` | 学新技能的切入点 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | 有过相关经验的人会按经验作答，可能测成熟悉度而非偏好 | 初稿；help 已限定“最近学着上手的事”，降低回忆难度 保留 |
| TF-04 | 主测 | TF | 看法被当面反驳（被反驳时的关注点） | 对方为什么这么在意这件事 | `F` | 对方的理由在哪里不成立 | `T` | 被当面反驳时的关注点 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 可能被读成“接不接受批评”的态度题 | 经左右镜像互换以修正 TF 维 6:6 平衡；两侧都不含对错评价 保留 |
| JP-04 | 主测 | JP | 事情快做完的时候（事情收尾的节奏） | 先告一段落，剩下的地方以后再改 | `P` | 再多留一点时间，接着改下去 | `J` | 事情快做完时的收束习惯 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “以后再改”容易被读成拖延、不负责 | 初稿改写；刻意用“告一段落 / 再多留一点时间”，与勤奋度脱钩 建议改写 |
| EI-05 | 主测 | EI | 忙完一天之后（精力恢复） | 和熟悉的人待着更让我缓过来 | `E` | 自己待一会儿才真正缓过来 | `I` | 忙碌一天结束后的回血方式 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | “和熟悉的人待着”可能被读成依赖他人 | 初稿；限定为“事情排得比较满的日子结束时” 保留 |
| SN-05 | 主测 | SN | 新点子刚被提出（对新点子的第一反应） | 先说它还能延伸出哪些做法 | `N` | 先说它能用在哪些具体场景 | `S` | 新点子刚被提出时的第一反应 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “具体场景 / 延伸做法”都偏中性，但可能被读成保守与激进 | 初稿改写；去掉“有没有先例”这类带评价的表述 建议改写 |
| TF-05 | 主测 | TF | 一群人商量事情（做取舍的优先项） | 先把标准说清楚，再按标准定 | `T` | 先看大家各自的想法和感受 | `F` | 一群人商量事情时的入手顺序 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | 这是全卷最容易被读成“讲道理 vs 讲人情”的题，可能被当成价值观题 | 初稿；需真人确认两侧都算正常做法，建议优先试测并准备改写 建议改写 |
| JP-05 | 主测 | JP | 面对还没到期的决定（决定与保留选项） | 早点定下，心里就不用再挂着 | `J` | 多留几天，说不定有更合适的 | `P` | 期限还早的决定 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | “早点定下”被读成果断，“多留几天”被读成犹豫 | 初稿；help 明确“必须二选一”，排除“还没想好”这类现实限制 保留 |
| EI-06 | 主测 | EI | 复杂问题摆在面前（思考方式） | 找人一起讨论，边聊边理 | `E` | 自己安静琢磨，想通再说 | `I` | 复杂问题的处理方式 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | “找人讨论”可能被读成能力不足 | 初稿；两侧都描述有效的思考路径 保留 |
| SN-06 | 主测 | SN | 接手一件陌生的事（对细节与整体的注意力） | 先知道大方向，细节自己摸 | `N` | 先有一份说清步骤的说明 | `S` | 接手陌生事情时的准备方式 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “先要知道大方向”可能被读成不踏实 | 初稿；把 S 侧写成“有一份说清步骤的说明”，是资源而非态度 保留 |
| TF-06 | 主测 | TF | 两边都有理由的争执（冲突处理） | 分清楚哪边的道理更站得住 | `T` | 先照顾双方在意的和关系本身 | `F` | 两边都有理由的争执 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | 易被读成“冷血 vs 和事佬” | 初稿；把 F 侧写成“照顾双方在意的”，不写成“和稀泥” 保留 |
| JP-06 | 主测 | JP | 手上攒着几件事（安排周末/任务的方式） | 排个先后，做完一件再开下一件 | `J` | 几件交替着做，看当时的状态 | `P` | 手上攒着几件事时的推进方式 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | “交替着做”容易被读成不专注、效率低，是本维最需要试测的题之一 | 初稿；建议优先试测，必要时把差异改成“推进顺序的确定性”而非效率 保留 |
| EI-07 | 主测 | EI | 休息时想要的动静（社交场合的选择） | 有些热闹和说话声在周围 | `E` | 安静一点，最好没人来打扰 | `I` | 空出来的下午想要的动静 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | “有些热闹”可能被读成爱玩，与 E/I 构念偏差 | 初稿；把问题落在“环境由你挑”，排除被现实条件限制的情形 建议改写 |
| SN-07 | 主测 | SN | 回想去过的一个地方（描述一件事的方式） | 那个地方给我的整体感觉 | `N` | 当时看到听到的画面和细节 | `S` | 回想去过的地方时想起什么 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “整体感觉”可能被读成更文艺或有文化 | 初稿改写；两侧都只描述回忆起什么，不评价深浅 保留 |
| TF-07 | 主测 | TF | 安排集体活动（做取舍的优先项） | 先把要求列出来，再挑合适的做法 | `F` | 先问大家想要什么，再凑出做法 | `T` | 张罗集体活动时的顺序 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 可能被读成“民主 vs 独断” | 初稿；把 T 侧写成“先列要求”，是工作方法而非态度 建议改写 |
| JP-07 | 主测 | JP | 一天里空出半天（安排周末/任务的方式） | 随手看看当时想做什么 | `P` | 前一天就想好要做哪几件事 | `J` | 突然多出半天时的默认状态 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “随手看看”可能被读成没有规划能力 | 初稿；两侧都是对自由时间的正常处理方式 保留 |
| EI-08 | 主测 | EI | 坐进一群正在聊的人（社交场合的选择） | 听一会儿，有把握了再开口 | `I` | 先接上话，聊着聊着就有了 | `E` | 加入正在聊的一群人 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | 与 EI-03 场景接近，可能被读成社交意愿而非精力方向；注意别测成重复题 | 初稿；与 EI-03 的区别在“已经在聊”的既有话题，试测时需复述验证 保留 |
| SN-08 | 主测 | SN | 讨论一件很具体的事（对新点子的第一反应） | 把已经掌握的情况摆清楚 | `S` | 顺着可以想到的关联往外推 | `N` | 具体的讨论被推进到哪一层 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | “往外推关联”可能被读成思维活跃，构成褒贬差 | 初稿；help 限定“需要把现状说明白的讨论”，减少场景歧义 保留 |
| TF-08 | 主测 | TF | 发现说法和事实对不上（被反驳时的关注点） | 先把说法改到和事实一致 | `T` | 先想怎么讲对方才接得住 | `F` | 说法与事实不符时的第一动作 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | 容易被读成“说话直不直”，与 F/T 的部分重叠但不等价 | 本卷把此题的 facet 明确为“被反驳时的关注点”，与 TF-04 的“被反驳”场景相邻，试测需比较两题是否同义 保留 |
| JP-08 | 主测 | JP | 别人临时来约（行程变化的适应） | 先看这一刻想不想去 | `P` | 先想今天原定的安排要不要动 | `J` | 别人当天临时来约 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “看这一刻想不想去”可能被读成随意、不靠谱 | 初稿；两侧都是正常回应方式，help 说明“时间就在今晚” 保留 |
| EI-09 | 主测 | EI | 连着几天和人打交道（精力恢复） | 中间插一段独处就恢复过来 | `I` | 有人一起反而越待越有劲 | `E` | 连着几天社交之后的恢复条件 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | “越待越有劲”可能被读成更受欢迎 | 初稿；两侧都描述恢复效果，不描述社交质量 保留 |
| SN-09 | 主测 | SN | 一件小事出了岔子（对细节与整体的注意力） | 先想它和前后哪些环节有关 | `N` | 先看这一步具体是哪里错了 | `S` | 小岔子出现时的排查顺序 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | 可能被读成“会不会查问题”的能力差异 | 初稿；把两侧都写成查原因的合理入口 保留 |
| TF-09 | 主测 | TF | 评价两个都做得不错的结果（看人做事） | 各自做得怎么样，合乎原来的要求吗 | `T` | 各自在这件事上投入了多少心思 | `F` | 评价两个都不错的结果 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | “花了多少心思”容易被读成看重人情；本题也是 T/F 高风险题 | 初稿改写；去掉“达标了吗”这类评判味强的说法，改为“合乎原来的要求吗” 保留 |
| JP-09 | 主测 | JP | 计划中途冒出别的事（事情收尾的节奏） | 先把手头这件事按原样做完 | `J` | 先转向那件事，回头再接着说 | `P` | 计划中途冒出别的事 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | “先转向那件事”可能被读成没条理 | 初稿改写；场景限定为“不算急事”，避免与优先级判断混淆 保留 |
| EI-10 | 主测 | EI | 准备把自己的看法讲出来（思考方式） | 先在对话里试一下，说到哪算哪 | `E` | 先在心里过一遍，再完整说出来 | `I` | 讲看法之前的准备程度 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | 可能被读成“准备充分不充分”的能力评价 | 初稿；两侧都是正常的表达准备方式，无优劣 保留 |
| SN-10 | 主测 | SN | 听人介绍一个没做过的事（学新东西的入口） | 它本身会怎么一步步发生 | `S` | 它和我知道的哪些事对得上 | `N` | 听人讲没做过的事时，先对什么产生反应 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | “和我知道的哪些事对得上”可能被读成保守 | 初稿；限定“你还没做过的事”，降低经验差异 保留 |
| TF-10 | 主测 | TF | 有人带着难处来找你（给建议的方式） | 他当时遇到的情况是怎样的 | `T` | 他心里的感受和在意的东西 | `F` | 有人带着难处来找你时的第一落点 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | 与 TF-02 场景相邻，存在同义反复风险，试测需比较两题的复述是否区分 | 初稿；保留但列入 facet 重叠观察名单 保留 |
| JP-10 | 主测 | JP | 一件事暂时做完（事情收尾的节奏） | 就让它这样，留些地方以后再看 | `P` | 顺手把还能改的地方改掉 | `J` | 一件事暂时告一段落之后 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “顺手把还能改的地方改掉”容易被读成认真，形成褒贬差，本维优先试测 | 初稿改写；把“松一口气 / 顺一遍”换成“让它这样 / 顺手改掉”，刻意去掉情绪与勤奋线索 建议改写 |
| EI-11 | 主测 | EI | 分享一件自己的好事（社交场合的选择） | 先自己高兴一阵，再挑人讲 | `I` | 马上找几个人讲一遍 | `E` | 得到好消息时的第一反应 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | “马上讲一遍”可能被读成外向开朗的人格标签 | 初稿；限定在“刚得到消息的那一刻”，是瞬时反应而非性格描述 保留 |
| SN-11 | 主测 | SN | 看待手边正在做的事（对新点子的第一反应） | 它最后会通向哪里 | `N` | 现在这一步可以怎么做扎实 | `S` | 对待手边正在做的事的关注点 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “它最后会通向哪里”可能被读成有远见 | 初稿；两侧都描述关注点，不评价格局 保留 |
| TF-11 | 主测 | TF | 两个人都想把事做好（冲突处理） | 谁的做法更经得起推敲 | `T` | 谁在这件事上更被为难 | `F` | 两个人都投入却做法不同 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | “更被为难”容易被读成同情心评分 | 初稿；两侧都承认对方投入，避免把 T 侧写成只顾对错 保留 |
| JP-11 | 主测 | JP | 有几个选项摆在眼前（决定与保留选项） | 先挑一个最合适的用起来 | `J` | 先不挑，等看得更清楚一点 | `P` | 几个都还行的选项摆在眼前 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | “先不挑”可能被读成犹豫不决 | 初稿；help 限定“都还行的选项”，排除信息不足导致的客观无法选择 建议改写 |
| EI-12 | 主测 | EI | 事情没做成之后（被打断后的反应） | 自己待一会儿，慢慢消化掉 | `I` | 找人说说，说着说着就没那么闷 | `E` | 结果不如意时的处理路径 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | “找人说说”可能被读成依赖或情绪外露 | 初稿；场景限定为“心里有点堵”的轻量受挫，避免与心理健康议题混同 建议改写 |
| SN-12 | 主测 | SN | 学一样东西学过一阵（学新东西的入口） | 顺着兴趣往深处探，练出新路 | `N` | 把基础反复练熟，先稳下来 | `S` | 学了一阵之后往哪边走 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “把基础练熟”容易被读成保守 | 初稿；两侧都写成持续推进的方式，不评价成就 建议改写 |
| TF-12 | 主测 | TF | 几个人一起评一个做法（冲突处理） | 先把做法本身说通，再看情绪 | `F` | 先让在场的人把话说完再接 | `T` | 几个人一起评一个做法 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 容易被读成“会不会带团队、顾不顾面子” | 初稿；把 T 侧写成“先把做法本身说通”，不写成否定他人 建议改写 |
| JP-12 | 主测 | JP | 一个安排做到一半（行程变化的适应） | 按原样走完，中途不折腾 | `J` | 中途想到更好的就换成新的 | `P` | 安排做到一半冒出新想法 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | “中途不折腾”容易被读成死板 | 初稿；把 P 侧写成“换成新的”，不写成“随便改主意” 建议改写 |
| EI-C1 | 澄清 | EI | 和几个人同桌吃饭（社交场合的选择） | 顺着话题一直聊下去 | `E` | 主要听，偶尔接一两句 | `I` | 饭桌上几个人随口聊着 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | “主要听”可能被读成不合群 | 澄清题，场景比主测更具体、更短，用于主测 E/I 接近时补证据 建议改写 |
| EI-C2 | 澄清 | EI | 排队时旁边有人搭话（社交场合的选择） | 接上几句就好，等轮到自己 | `I` | 顺势聊起来，队伍慢了也不在意 | `E` | 排队时旁边有人搭话 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | 容易被读成怕生或社恐，需真人确认不含胆量暗示 | 澄清题；两侧都限定为几句寒暄的强度 保留 |
| EI-C3 | 澄清 | EI | 接起一个电话（思考方式） | 先想一下要说什么再接 | `I` | 直接接起来，边说边想 | `E` | 电话响起时先想还是先接 | 右端 `E` 为正极 → 选右侧时 `direction = +1`；左端 `I` 为负极，选左侧时 `direction = −1`。本维负极 `I`、正极 `E`。 | “先想一下要说什么”可能被读成社交焦虑 | 澄清题；help 明确“不是必须马上接”的情形，排除现实限制 保留 |
| EI-C4 | 澄清 | EI | 事情堆在一起的时候（思考方式） | 先自己理清顺序再动手 | `E` | 先找人聊两句再开始 | `I` | 几件事压在一起时先做什么 | 左端 `E` 为正极 → 选左侧时 `direction = −1`；右端 `I` 为负极，选右侧时 `direction = +1`。本维负极 `I`、正极 `E`。 | “先找人聊两句”可能被读成不能独立做事 | 澄清题；两侧都是启动方式，不评价独立性 建议改写 |
| SN-C1 | 澄清 | SN | 去一个没去过的地方（对细节与整体的注意力） | 先记住路上看到的标志和路口 | `S` | 先弄清它在城里的大致方位 | `N` | 自己找到没去过的地方 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | 方向感好坏会被混入，需确认用户按偏好而非能力作答 | 澄清题；只用“先记住标志 / 先弄清方位”，不涉及能不能找到 建议改写 |
| SN-C2 | 澄清 | SN | 买一件没买过的东西（对细节与整体的注意力） | 先看用过的人怎么说、参数如何 | `S` | 先想清楚自己到底需不需要 | `N` | 买不太熟的东西之前的动作 | 右端 `N` 为正极 → 选右侧时 `direction = +1`；左端 `S` 为负极，选左侧时 `direction = −1`。本维负极 `S`、正极 `N`。 | “先看参数评价”可能被读成会过日子 | 澄清题；避免出现价格与预算等现实限制 建议改写 |
| SN-C3 | 澄清 | SN | 朋友问周末过得怎么样（描述一件事的方式） | 说这两天挺松快、状态不错 | `N` | 把去了哪、做了什么讲一遍 | `S` | 回答“周末过得怎么样” | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “说状态”可能被读成敷衍 | 澄清题；经左右镜像后 N 在左，两侧都是正常回答方式 建议改写 |
| SN-C4 | 澄清 | SN | 别人给你讲一件麻烦事（学新东西的入口） | 先听出这事的关键卡在哪儿 | `N` | 先问到具体是哪一步出了问题 | `S` | 听人讲还没解决的麻烦 | 左端 `N` 为正极 → 选左侧时 `direction = −1`；右端 `S` 为负极，选右侧时 `direction = +1`。本维负极 `S`、正极 `N`。 | “先听出关键”可能被读成更会解决问题 | 澄清题；两侧都是合理的倾听起点 建议改写 |
| TF-C1 | 澄清 | TF | 朋友临时向你借一笔钱（做取舍的优先项） | 先想这件事合不合理、还得上吗 | `T` | 先想他为什么会开这个口 | `F` | 朋友临时开口求助 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | 金钱场景可能触发实际压力与真实经历差异，需真人确认不引发不适 | 澄清题；措辞限定“临时开口”，不涉及金额 保留 |
| TF-C2 | 澄清 | TF | 看到一条新出的规定（面对规则与例外） | 它会不会让一些人变得很难做 | `F` | 它能不能解决原来那个问题 | `T` | 看到一条新出的规定 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 可能被读成“支持还是反对规定”的立场题 | 澄清题；两侧都是评价角度，不涉及政治或制度立场 保留 |
| TF-C3 | 澄清 | TF | 张罗一次多人聚会（做取舍的优先项） | 先把时间地点定死，好安排 | `F` | 先问一圈谁方便，再定下来 | `T` | 张罗一次多人聚会 | 左端 `F` 为正极 → 选左侧时 `direction = −1`；右端 `T` 为负极，选右侧时 `direction = +1`。本维负极 `T`、正极 `F`。 | 与 TF-07 场景相近，需比较两题是否同义 | 澄清题；把 T 侧写成“先定死好安排”，不写成不顾他人 建议改写 |
| TF-C4 | 澄清 | TF | 话没错但说得不好听（被反驳时的关注点） | 先把准确的说法讲清楚 | `T` | 先看现在怎么把话说开 | `F` | 话没错但说得不好听 | 右端 `F` 为正极 → 选右侧时 `direction = +1`；左端 `T` 为负极，选左侧时 `direction = −1`。本维负极 `T`、正极 `F`。 | 容易被读成“听不听得进批评” | 澄清题；两侧都承认对方说的是对的，只区分关注点 保留 |
| JP-C1 | 澄清 | JP | 出门采购的时候（安排周末/任务的方式） | 到了看有什么合适的再买 | `P` | 出门前先列好要买什么 | `J` | 出门采购 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “到了再看”可能被读成没条理；清单也可能被读成会过日子 | 澄清题；help 明确“清单可有可无”，去掉必要性差异 保留 |
| JP-C2 | 澄清 | JP | 一趟不太远的出行（行程变化的适应） | 先看清车怎么走、几点到 | `J` | 大概知道方向，到了再说 | `P` | 一趟不太远的出行 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | 容易与方向感混淆，需确认按偏好而非能力作答 | 澄清题；场景限定为“不太远”，避免长途规划的现实负担 建议改写 |
| JP-C3 | 澄清 | JP | 几件小事排在今天（安排周末/任务的方式） | 一件接一件按顺序做完 | `J` | 先做哪个看当时顺不顺手 | `P` | 今天几件小事 | 右端 `P` 为正极 → 选右侧时 `direction = +1`；左端 `J` 为负极，选左侧时 `direction = −1`。本维负极 `J`、正极 `P`。 | 与 JP-06 相近，需比较两题区分度 | 澄清题；把差异收窄为“顺序固定还是看当时顺手” 保留 |
| JP-C4 | 澄清 | JP | 事情只差一点收尾（事情收尾的节奏） | 先放着，等哪天想接着做再说 | `P` | 顺手把它做完，不留个尾巴 | `J` | 只差一点收尾的事 | 左端 `P` 为正极 → 选左侧时 `direction = −1`；右端 `J` 为负极，选右侧时 `direction = +1`。本维负极 `J`、正极 `P`。 | “先放着”容易被读成拖延，是本维澄清题里风险最高的一题 | 澄清题改写；把“以后有空再弄”改为“等哪天想接着做再说”，去掉责任暗示 建议改写 |

## 2. 每维左右方向计数核对（契约 §2.3 硬校验）

| 维度 | 负极 / 正极 | 主测题数 | 主测 leftPole=负极 | 主测 leftPole=正极 | 主测是否 6:6 | 澄清题数 | 澄清 leftPole=负极 | 澄清 leftPole=正极 | 澄清是否 2:2 |
|---|---|---|---|---|---|---|---|---|---|
| EI | `I` / `E` | 12 | 6 | 6 | 是 | 4 | 2 | 2 | 是 |
| SN | `S` / `N` | 12 | 6 | 6 | 是 | 4 | 2 | 2 | 是 |
| TF | `T` / `F` | 12 | 6 | 6 | 是 | 4 | 2 | 2 | 是 |
| JP | `J` / `P` | 12 | 6 | 6 | 是 | 4 | 2 | 2 | 是 |

四维主测与澄清全部满足契约要求：**是**。
校验上限（契约 §2.4 第 5 条要求校验器拒绝不平衡的包）：本文件当前状态为每维主测 6:6、澄清 2:2。

## 3. 每维 facet 覆盖表

契约 §2.4 第 7 条要求每维覆盖 ≥3 种不同 `facet`（防 12 题同义反复）。

| 维度 | 主测 facet 数 | 主测 facet 分布 | 澄清 facet 分布 | 判定 |
|---|---|---|---|---|
| EI | 4 | 精力恢复×3、思考方式×3、社交场合的选择×4、被打断后的反应×2 | 社交场合的选择×2、思考方式×2 | 通过（≥3） |
| SN | 4 | 学新东西的入口×4、描述一件事的方式×2、对细节与整体的注意力×3、对新点子的第一反应×3 | 对细节与整体的注意力×2、描述一件事的方式×1、学新东西的入口×1 | 通过（≥3） |
| TF | 6 | 做取舍的优先项×3、看人做事×2、面对规则与例外×1、被反驳时的关注点×2、冲突处理×3、给建议的方式×1 | 做取舍的优先项×2、面对规则与例外×1、被反驳时的关注点×1 | 通过（≥3） |
| JP | 4 | 安排周末/任务的方式×3、决定与保留选项×3、行程变化的适应×3、事情收尾的节奏×3 | 安排周末/任务的方式×2、行程变化的适应×1、事情收尾的节奏×1 | 通过（≥3） |

说明：契约的 ≥3 facet 要求针对**主测**；澄清题因每维仅 4 题，EI / SN 为 2 种 facet，不构成契约违规，但第 5 节把这两维的澄清题列入试测观察项。

## 4. 内测待审校声明（**必须随本表一起归档**）

**本题目库未经真人试测，无信度与效度证据。**

具体说明：

1. 本题库为 2026-09-16 生成的**中文原创草案**，`reviewStatus` 全部为 `draft_review_pending`，`provenance` 已逐题标注"未做真人审读与试测"。参考的只是 Jung 四维偏好框架的公开常识描述，**未使用 MBTI 官方、16Personalities、OEJTS 的任何原题，也未做逐句换词**。
2. **没有任何统计证据**：无内部一致性（α / ω）、无重测信度、无因子结构、无与任何外部量表的效标关联、无项目区分度（D 值）、无选项分布与天花板/地板效应数据。本表第 2、3 节的"通过"只表示**符合契约的结构性校验**（题数、题号、极点配对、左右平衡、facet 种类、字数），**不是心理测量学意义上的合格**。
3. 契约 §2.4 的发布期校验只能拦住结构性错误，**拦不住语义错误**。两侧是否都正常可接受、是否隐含道德或能力高低、用户是否会按"现实限制"而非"自然偏好"作答，这些问题只能由真人试测回答。
4. 按产品方案 §4.5：至少安排 5–8 名目标用户试做并**用自己的话复述题意**，同一题被两人独立误解其核心意思即列入必改；目标为首轮至少 90% 题目能被多数参与者正确复述。**不允许开发代理代填"真人审校通过"。**
5. 在完成上述试测前，本题库只能作为**内测预览**交付，内容状态必须保持 `draft_review_pending`，不得对外称已验证。
6. 本表的"可能的歧义/误读风险"与"修改原因/创作说明"两列是**创作方的自述假设**，属于待验证的猜想，不是审校结论。

## 5. 最需要优先真人试测的 8 道题

选取标准：① 位于 T/F 与 J/P 这两个最易被写成"好/坏"的维度；② 两侧语义距离近、易被同一人同时认同；③ 可能被读成能力、勤奋、道德评价；④ 与其他题存在同义反复风险。

| 优先级 | 题号 | 维度 | 为什么必须优先试测 |
|---|---|---|---|
| 1 | `TF-05` | TF | "先立标准 / 先看大家想法"是全卷最容易被读成"讲道理 vs 讲人情"的价值观题，用户可能认为只有一侧才"正确"，从而失去偏好作答的意义 |
| 2 | `TF-09` | TF | 评价他人成果时"合乎要求 / 投入心思"两侧都正面，极易被同时认同；且"投入心思"可能诱发言不由衷的社会赞许作答 |
| 3 | `JP-06` | JP | "串行做完 / 交替着做"最容易被读成专注度与效率差异，即勤奋 vs 拖延的变体，违反 J/P 写作禁令 |
| 4 | `EI-04` | EI | 与 E/I 构念距离最远：被打断后是"先听完"还是"顺势聊"，更像沟通礼貌问题，可能整题无效，需确认是否改写或替换 |
| 5 | `SN-04` | SN | 学过类似技能的人会按**经验**而非**偏好**作答，可能测成熟悉度；需复述确认用户是否理解成"你更想先要什么" |
| 6 | `TF-12` | TF | "先让内容说通 / 先让人把话说完"容易被读成"会不会照顾面子、会不会带团队"，需确认没有把 F 写成"善良"、T 写成"冷漠" |
| 7 | `JP-10` | JP | "让它这样 / 顺手把还能改的地方改掉"中后一侧带明显的"认真"色彩，存在褒贬差，可能把 J/P 测成负责程度 |
| 8 | `JP-C4` | JP | 澄清题里风险最高："先放着 / 顺手做完"最接近拖延叙事；澄清题用于主测接近的人，一旦有褒贬差会直接决定边界维度的翻转方向 |

补充观察项（若首轮试测名额允许，建议一并覆盖）：

- `TF-03`、`TF-04`：规则与例外、被反驳时的关注点，均有被读成"通融=好人""接受批评=成熟"的风险。
- `TF-02` 与 `TF-10`：两题场景相邻（朋友来商量 / 有人带难处来找你），需验证是否同义反复。
- `SN-03` 与 `SN-07`：读材料抓什么、回想地方想起什么，同属"细节 vs 整体"，需验证区分度。
- `EI-03` 与 `EI-08`：半陌生场合与加入既有话题，均涉社交进入方式，需验证不被复述成同一题。
- `EI-C1`–`EI-C4`、`SN-C1`–`SN-C4`：两维澄清题各只覆盖 2 种 facet，且澄清题直接参与边界维度判定，其质量对最终类型与候选排序影响很大。
- `SN-C1`、`JP-C2`：涉及空间与路线，需确认用户在按偏好作答，而非按"我方向感好不好"作答。

## 6. 本次左右平衡调整记录（供复核者对照）

为满足契约 §2.3（每维主测 6:6、澄清 2:2），编制过程中对部分题做**左右镜像互换**：即 `textLeft` 与 `textRight` 互换，同时 `leftPole` 与 `rightPole` 互换。镜像后**题目含义、维度归属、facet、order 均不变**，仅改变哪一端显示在左侧；按契约 §4 的计分公式，镜像不会改变同一偏好者的最终方向（契约 CASE-17 即校验此性质）。

涉及镜像的题：`EI-02`、`EI-12`、`SN-03`、`SN-04`、`SN-05`、`SN-06`、`SN-08`、`SN-10`、`SN-12`、`TF-01`、`TF-02`、`TF-03`、`TF-04`、`TF-07`、`TF-11`、`TF-12`、`JP-04`、`JP-07`、`JP-09`、`JP-10`、`EI-C2`、`EI-C4`、`SN-C3`、`SN-C4`、`TF-C2`、`TF-C3`、`JP-C1`。

复核方式：对上述任一题，把两侧文字与极点同时互换后，四维 `S`、`m`、方向、边界标记、候选与 cost 应完全不变；若发现互换后结论改变，说明该题的 `textLeft/textRight` 与 `leftPole/rightPole` 已不一致，属于契约 §2.4 第 4 条的失败项，必须修正。

## 7. 本表未做的事

- 未做真人试测、未做认知访谈、未做统计检验；
- 未写 16 型报告内容（属 `type-reports-zh-v1`，另一份内容包）；
- 未修改题库之外的任何项目文件；
- 未对旧 `oejts32` / `ipip50` 内容做任何复用、映射或改写。

## 8. 补充审读发现：4 道题的题面方向与所标极点不一致（建议优先复核）

逐题核对 `textLeft/textRight` 与 `leftPole/rightPole` 的语义是否对应时，发现 4 道题的**文字与所标极点相反**。这类问题不会触发契约 §2.4 的校验（该条只要求两极属于本维且互不相同），但会让**作答方向被反向计分**：符合某一极的人选了那一侧，反而被记到相反的一极。建议在试测前处理。

| 题号 | 维度 | textLeft（所标极点） | textRight（所标极点） | 不一致之处 |
|---|---|---|---|---|
| `TF-07` | TF | 先把要求列出来，再挑合适的做法（`F`） | 先问大家想要什么，再凑出做法（`T`） | 列要求、按条件挑做法属"标准优先"，问大家想要什么属"人的处境优先"，与所标的 F/T 相反 |
| `TF-12` | TF | 先把做法本身说通，再看情绪（`F`） | 先让在场的人把话说完再接（`T`） | "把做法说通"偏 T，"让在场的人把话说完"偏 F，与所标的 F/T 相反 |
| `TF-C3` | TF | 先把时间地点定死，好安排（`F`） | 先问一圈谁方便，再定下来（`T`） | "定死好安排"偏效率与结构，"问一圈谁方便"偏照顾人，与所标的 F/T 相反；且与 `TF-07` 场景同族 |
| `EI-C4` | EI | 先自己理清顺序再动手（`E`） | 先找人聊两句再开始（`I`） | 先内部整理偏 I，先找人说偏 E，与所标的 E/I 相反 |

**修复方式（二选一，都需要形成新的内容包版本）**

1. 只把该题的 `textLeft` 与 `textRight` 对调，`leftPole`/`rightPole` 保持不动。这样文字与极点重新对应，且**不改变**该维的左右平衡（左侧极点仍是原来那个）。
2. 只把 `leftPole` 与 `rightPole` 对调，文字保持不动。这样也会恢复对应关系，但**会改变**该维的左右平衡，必须重跑契约 §2.3 的 6:6（主测）/ 2:2（澄清）检查。

注意：这与本表 §6 记录的"镜像互换"不是同一个操作。镜像互换是文字与极点**同时**对调，因此不会产生、也不会修复不一致；本次发现的问题是只动了一侧留下的。

**复核建议**

- 修好之后，重新逐题核对一遍全部 64 题的 `textLeft` 是否都落在 `leftPole` 一侧，避免同类问题在别处残留。
- 这 4 道题的 `direction` 会随修复翻转，凡是用到这些题目的计分夹具（`CASE-*`）在修复后都需重新生成或核对期望值。
- 在修复前，`TF-07`、`TF-12`、`TF-C3` 会让偏 T 与偏 F 的作答在这三题上被反向计入，`EI-C4` 同理影响 E/I 的澄清结果。
