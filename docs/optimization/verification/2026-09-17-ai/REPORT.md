# 真实浏览器验收：报告页 AI 分析面板（2026-09-17）

- 被验收地址：`http://127.0.0.1:5174`（第 9 轮收尾回归（新增 /admin 路由、账号页管理入口、useAdminProbe 后））
- AI 是否开启：`True`；是否 mock 适配器：`True`
- 使用账号：`aivf_06a582d5`（密码为脚本生成的合成值，未记录）
- 结论：**PASS**（PASS 31 / FAIL 0 / SKIP 0）

## 观察记录

- GET /ai/status → enabled=True mock=True
- 等待 AI 结果 3s
- 生成后额度文案：'今天还可以生成 1 次。'

## 说明

- 本脚本只做注册 → 建测评 → 作答 → 交卷 → 读报告 → 生成 AI 分析；不调用删除/注销/导出。
- 断言的是页面上真实出现的东西（`data-ai-*` 钩子与文案），不是内部 state。
- AI 结果来自 mock 适配器（若 `mock=true`）：不联网、不用真实 key、带 `mock: true` 标记，
  页面必须显著标注「演示数据」。
