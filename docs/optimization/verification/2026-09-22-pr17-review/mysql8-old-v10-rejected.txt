# ⚠ 更正（2026-09-22）：本文件是一次**失败的探测方法**留下的，不能当证据用。
#   原因：命令里同时写了 `< h2-old-v10-ddl.sql` 与 `</dev/null`，`</dev/null`
#   后出现、覆盖了前者，mysql 读到的是空输入 → 退出码 0 是「读了空文件」，不是
#   「旧版 DDL 在 MySQL 上执行成功」。正确的探测结果见 mysql8-original-v10-rejected.txt。
#   （保留本文件是为了不抹掉自己的错误过程；结论请以更正后的文件为准。）

# 在真实 MySQL 8.4 上执行旧版 V10 的建表语句（列名 release + utf8mb4_0900_as_cs）
# 命令：mysql -h127.0.0.1 -uroot -p*** <typeme_r32_probe> < h2-old-v10-ddl.sql
# 采集时间：2026-09-22T15:16:30+08:00
mysql: [Warning] Using a password on the command line interface can be insecure.
OLD_V10_ON_MYSQL_EXIT=0
(探测库已删除)
