# 管理员与邀请注册验收 · 2026-09-18

## 本轮结果

- 后端：JDK 21，`mvn.cmd -q -f backend/pom.xml test '-Dtest=*,!AccountSqlDialectMySqlIT,!AiSqlDialectMySqlIT,!ConcurrencyMySqlIT'`，退出码 0。本次执行 332 项，331 通过、0 失败、0 错误、1 跳过。
- 跳过的是 `AssessmentPackageLoadingTest.emptyDirectoryDoesNotBlockV1Startup`：内容目录已经有包，不满足该测试的「空目录」前提，非本轮新增跳过。
- 明确排除了 3 类会建删真实 MySQL 数据库的测试。测试环境使用隔离 H2、mock AI；执行 Maven 子进程前移除了该子进程的 `DEEPSEEK_API_KEY` / `TYPEME_AI_API_KEY` 环境变量，以免「未配置密钥」测试继承本机环境。没有读取、输出或更改持久环境中的密钥。
- 前端：`npm.cmd run typecheck`、`npm.cmd test -- --run`、`npm.cmd run build` 均退出 0；38 个测试文件、902 项测试通过。
- 浏览器：`python scripts/browser-verify-admin-members.py`，65 项检查通过，320 / 390 / 1440 宽度，0 JavaScript 错误、0 未定义 API 请求。所有 API 已拦截，合成账号、邀请码和报告均不是真实用户数据。
- 人工查看了桌面管理页、320 窄屏管理页与 390 注册页截图，检查排版、换行、焦点和主要操作布局。

## 核心覆盖

| 范围 | 验证内容 |
| --- | --- |
| 默认管理员 | 缺密码不创建、正常创建及可登录、编码存储、重启不改密码、占名不提权、禁止普通注册保留名、配置对象隐藏凭据 |
| 邀请注册 | 缺码/非法/过期/撤销拒绝、一次使用、并发只能成功一个、失败无残留账号、用户名冲突不消耗码 |
| 邀请管理 | 仅管理员读写、CSRF、过期时间边界、只存校验值、列表不泄露码、已用邀请不能撤销、注销不复活旧码 |
| 报告权限 | 管理员可查看目标用户的进度及原快照、错误用户与报告组合为 404、普通用户越权 404、普通用户访问管理路由 403 |
| AI 额度 | 单独分配及继承默认、0 禁用新请求/重试、调低不清零、越额拒绝不多占预算、不同用户隔离、全站预算和原有 AI 回归 |
| 页面 | 生成/隐藏/撤销邀请、成员翻页、保存 0 额度、进度、十六型/平分/大五报告、加载失败重试、403 与会话失效清除数据、注册披露及键盘焦点、三种宽度无横向溢出 |

详细断言：[browser-checks.json](browser-checks.json)。

截图：[桌面管理页](members-1440.png)、[窄屏管理页](members-320.png)、[手机注册页](register-390.png)、[十六型报告](report-jung-390.png)、[大五报告](report-bigfive-390.png)、[平分报告](report-tied-390.png)。

## 修正和未覆盖项

- 发现并修正管理员入口复用平台查询时的异常映射：报告归属不匹配保留 404，不再误报 500。
- 账号清理新增邀请关联清理；同步加强「所有清理步骤失败必须上报」的回归断言。
- 旧 Surefire 目录含以前的诊断报告；以上计数只统计本次执行时间内报告，不把历史错误或历史数量混入本次结果。
- 仓库 `git diff --check` 唯一空白错误位于既有改动 `docs/optimization/progress.md:2101` 的末尾空行，本轮没有修改它。
- 未迁移现有数据库、未创建真实管理员、未验证真实 MySQL 并发、未调用真实 AI、未打包/部署新的服务 jar。浏览器证据是构建后的静态前端加模拟接口；真实 HTTP 权限与事务来自独立 MockMvc/H2 测试。
