# 真实浏览器验收：**历史报告**能否被 AI 分析（2026-09-17）

- 被验收地址：`http://127.0.0.1:5174` （第 14 轮衍生：历史报告能否被 AI 分析）
- AI 是否开启：`True`；是否 mock 适配器：`False`
- 使用账号：`hist_abda60a7`（密码为脚本生成的合成值，未记录）
- 结论：**PASS**（PASS 18 / FAIL 0 / SKIP 0）

## 这次验收要回答的问题

用户问的是『历史报告也能分析吧』。代码层面：AI 面板挂在报告详情组件上，
创建路径只校验（幂等键 / AI 已开启且有 key / 报告属于本人），没有按报告时间设限。
但 `AiAnalysisPanel` 只在 `onMounted` 加载任务、没有 `watch(reportId)`，而父组件用
`v-if="reportId"` 挂载它 —— 从一份报告切到另一份时组件实例会被复用。
本脚本就是去查这件事在真实浏览器里到底会不会串台。

**结论：没有串台，但"没串台"的原因和我一开始的假设不同。** 真正的保护来自报告正文
外面的 `<template v-else-if="view">` 与加载态：换报告时 `reports.loading` 置位、
`view` 被清空，整块正文（含 AI 面板）被**销毁重建**，于是 `onMounted` 会重新执行。
换句话说 —— **是加载态顺手救了它，不是那个 `v-if` 挡住了问题**。
`AiAnalysisPanel` 至今没有 `watch(() => props.reportId)`；这条依赖是隐式的，
若将来有人把加载态改成"保留旧内容 + 局部骨架屏"（很常见的体验优化），
面板就会开始串台。本轮**没有**去改它（当前行为正确，且有真实浏览器断言守着），
只把这个隐患记进 backlog。

## 观察记录

- GET /ai/status → enabled=True mock=False
- 新报告的分析仍在进行中（本脚本不等它出结果）。
- 新报告的任务 id=ec79aca8…
- 老报告的任务 id=9a5a7321…

## 断言（页面真实出现的东西）

- PASS 18 条：
    - 后端已开启 AI（否则本脚本无意义）
    - 第一次测评产出报告
    - 第二次测评产出报告
    - 两份报告是不同的记录
    - 历史列表里至少两份报告
    - 老的那份报告在历史列表里
    - 新报告页有 AI 面板
    - 新报告页有生成入口
    - 新报告的 AI 任务被后端接受
    - 从历史列表点进了老报告
    - 老报告上没冒出本该属于新报告的分析（串台检测）
    - **对历史报告发起的分析被后端接受**
    - 两份报告的任务是两个不同的任务
    - 地址确实换成了新报告（页面未重载）
    - 换到新报告后显示的是**新报告自己**的任务
    - 地址切回了老报告（页面未重载）
    - 切回老报告后显示的是**老报告自己**的任务
    - 刷新后老报告仍显示自己那次分析（不丢、不串）

## 本脚本的边界

- **会真的调用外部模型**：脚本不等结果，但后端 `AnalysisWorker` 是**急切执行**的，
  任务落库即发起真实请求 —— "只验证能发起"**省不掉**调用与费用。
  一次运行 = **2 次真实外发**（新报告 1 + 老报告 1）。
  初版脚本文档曾写成『不消耗额度』，是**错的**；已在脚本与产物中更正。
  本次运行的 2 次外发由后端日志证实：`14:07:44 tokens=5124`、`14:07:54 tokens=5015`。
- 用的是脚本注册的一次性合成账号，作答为固定选项，不含任何真实个人信息。
- 脚本初版只数 `data-ai-job` 的个数，**那个断言没有区分力**（"1 个任务"既可能是本报告的、
  也可能是上一份残留的）。为此在 `AiAnalysisPanel` 上补了 `data-ai-job-id`，
  改为对比 jobId —— 本报告里那条"各显示自己的任务"才是真正能抓住串台的断言。
