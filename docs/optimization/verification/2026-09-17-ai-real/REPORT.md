# 真实浏览器验收：报告页 AI 分析面板 · **真实模型调用**（2026-09-17）

- 被验收地址：`http://127.0.0.1:5174`（真实 DeepSeek 调用（管理员后台配置的 key，mock=false））
- AI 是否开启：`True`；是否 mock 适配器：`False`（**真实调用外部模型服务**）
- 使用账号：`aivf_a9dd6229`（密码为脚本生成的合成值，未记录）
- 结论：**PASS**（PASS 30 / FAIL 0 / SKIP 0）

## 这次验收要回答的问题

第 9 轮结束时有一条明确的遗留：**真实 AI 调用（非 mock）从未被验证**，因为需要真实 key
与外发授权。同时本轮发现后台保存的 key 根本到不了 AI 模块（见 `progress.md` 第 10 轮），
修复后必须证明"管理员在后台填的 key 能被真实用出去"，而不只是日志里写着 `apiKeySource=db`。

## 前置事实（服务端日志，非推测）

```
AiRuntimeSettingsProvider : AI 设置已加载：enabled=true, model=deepseek-flash,
                            apiKeySource=db, mockMode=false, baseUrl=https://api.deepseek.com
```

`apiKeySource=db` 是修复后才会出现的取值：它证明 AI 模块**解开了管理员在后台保存的密文**。
修复前这里会是 `env`/`none`，并伴随 `无法解密（缺少 SecretCipher 实现…）` 的 WARN。

## 观察记录

- GET /ai/status → enabled=True mock=False
- 注意：这次 backend 不是 mock 模式，脚本会真的调用外部模型服务。
- 等待 AI 结果 12s
- 生成后额度文案：'今天还可以生成 1 次。'

## 断言（页面真实出现的东西）

范围确认与同意：报告页有 AI 面板 → 有生成入口 → 显示今日剩余次数 → 点生成先展开范围确认
（不是直接开始）→ 确认区如实列出会发送/不会发送 → 明确写出不发送用户名昵称 → 明确写出不发送
完整题库与完整答卷 → 未勾选时不能提交 → 勾选后可提交。

真实生成：提交后立刻进入「进行中」 → 任务**没有失败** → 拿到结果 → **不标注「演示数据」**
（真实模式的反向断言）→ 有「整体印象」→ 有分节正文 → 有「可以试试的具体做法」→
有「可以问问自己」→ 没有「有一部分没读出来」的提示 → 结尾保留定位说明 → 不显示内部字段。

刷新与额度：刷新后结果仍在、能读到这次任务；额度文案仍可读。

布局：320 / 1440 无横向溢出；320 下 AI 面板宽度不超过视口。

截图 4 张（含 390 结果页与 320 窄屏）。

## 本次调用实际外发了什么

由 `ReportInputBuilder` 构造、页面确认页逐条列出：四维方向/强度/是否边界、结果状态与候选方向、
**最多 8 条作答片段**（题号 + 左右字母 + 所选档位）、过程层摘要。
**不含**用户名/昵称/登录信息/完整题库/完整答卷/其他报告/内部编号。
本次用的是脚本注册的一次性合成账号与脚本作答，不含任何真实个人信息。

## 两处需要如实说明的地方

1. **产物更正**：本次运行后 `result.json` 的 `aiMock` 字段曾为 `true`，与观察记录里的
   `mock=False` 自相矛盾。原因是脚本 `finally` 分支写成 `report(None, username, True, True)`，
   把 mock 硬编码成 `True`。已修脚本（改用 `/ai/status` 实测值），并**更正了本次产物**的该字段。
   保留一个说假话的字段，比没有这个字段更糟 —— 任何依据它判断"这是不是演示数据"的人都会被误导。
2. **一次 403**：后端日志有一条 `api-error code=FORBIDDEN status=403`。它出现在注册后的
   权限探测路径上（脚本的 `probeAdminAccess()` 会访问受保护端点以判断是否管理员），
   属于**预期内的被拒**，不是本次流程失败；脚本 30 项断言全绿且 AI 任务成功。
