# TypeMe 问题清单（backlog）

> 本文件是**持续优化任务**的问题台账（见 `docs/DSH-持续优化提示词.md`）。
> 规则：每条必须有**可复现证据**（命令输出 / 截图 / 代码位置），并维护状态，
> 避免多轮重复寻找同一问题。历史文档里的"缺口清单"只作线索，必须重新核实后才进本表。
>
> 状态：`待办` / `进行中` / `已修待验` / `已闭环` / `需用户决定` / `仅记录`
> 优先级：P0 安全/数据/主流程阻断 > P1 口径冲突与状态不同步 > P2 高频页面体验 > P3 性能与维护

## 环境基线（2026-09-17 建立）

| 项 | 实测 |
|---|---|
| 仓库状态 | `git status --short` 干净；HEAD `1dbad2a` |
| 内容一致性 | 5 条 `--check` / 重复度脚本全部 exit 0（详见 progress.md） |
| 前端 typecheck | `vue-tsc --noEmit` exit 0 |
| 前端测试 | Vitest 17 文件 / 693 条通过、exit 0（与 implementation README 记载的 693 一致） |
| JDK | `D:\develop\jdk-21` 存在；PATH 上的 `java` 是 17（需显式设 `JAVA_HOME`） |
| MySQL | 本机 3306 未监听；`typeme_dev` 连接不可用（本轮未做任何数据库写操作） |

## 本轮新增（2026-09-17）

| ID | 问题 | 证据 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A1 | 顶栏导航把「开始测评」渲染了**两次**，同一个目标 `/assess` 相邻出现两个入口 | `frontend/src/App.vue` L204-210 与 L218-223 两个 `<RouterLink to="/assess">开始测评</RouterLink>`；前者的 `route.name` 高亮判断永远为假（`quizActive` 已经排除 assess / assess-attempt），后者才是原本要删的"旧版本测试"入口残留 | 顶栏出现重复入口；高亮态失效，"我现在在测评页"这件事在导航上不可见 | P2 | 已闭环（见 progress.md 第 1 轮） |
| A2 | 顶栏「关于」与「方法与隐私」两个文案**写反** | `App.vue` L228：`route.name === 'about' ? '方法与隐私' : '关于'` —— 停在关于页时反而显示"方法与隐私" | 导航文案与当前页面不符，用户以为还有第二个页面 | P2 | 已闭环（第 1 轮） |
| A3 | `ink-faint`（#7C8892）作为 12–13px 辅助文字时对比度 **3.35:1**（纸上）/ 3.62:1（白底），低于 WCAG AA 小字 4.5:1 | 计算脚本 `work/verify/contrast.mjs` 输出；`style.css` 的 `.fineprint`(12px)/`.caption`(13px) 与约 40 处 `text-ink-faint` 小字号用法 | 页脚署名、表单辅助说明与**校验错误提示**这类必须读到的字最难读 | P2 | 已闭环（第 2 轮） |
| A4 | 注册时**没有**免责声明同意项，后端也不校验 | `frontend/src/views/RegisterView.vue` 无勾选框；`docs/2026-09-16/verification/browser-acceptance.md` §"未能验证的部分" 1 与 `acceptance-evidence.md` §9.1 实测（`input[type=checkbox]` 为 0）；`RegisterRequest` 无该字段 | 产品方案与"数据存账号里"的告知义务缺一环；历史上有过被误当成已实现的情况 | P1 | 需用户决定（见下） |
| **A5** | **`GET /api/v3/attempts/{id}` 在 H2 上直接 500，答题页整页不可用**（跨库移植性缺陷） | 内存 H2 起真实后端复现：`POST /api/v3/attempts` → 201，紧接着 `GET /api/v3/attempts/{id}` → 500；日志 `java.lang.ClassCastException: class java.sql.Timestamp cannot be cast to class java.time.LocalDateTime`，位于 `AttemptService.detail(AttemptService.java:164)`（修复前）。根因：`queryForList` 走 `ColumnMapRowMapper` → `ResultSet.getObject(name)`，H2 2.x 返回 `Timestamp`、Connector/J 返回 `LocalDateTime`，而代码直接强转。同形写法另有 `ReportService.java:303`（`selfReflection`）与 `:357`（`compare`）。 | **P0**：真实用户在答题页看到「这份测评没能载入：请求 /attempts/xxx 失败（HTTP 500）」且无题可答；旧测试全绿也漏掉了它 | P0 | 已闭环（第 3 轮，含反向验证） |
| A6 | 320 宽窄屏下粘性顶栏占首屏 **24.5%**（报告页实测 139px / 568px） | `scripts/browser-verify-narrow-layout.py` 输出；原因为品牌行副标题 + 三个导航入口同时折行 | 常驻顶栏挤占矮机型首屏 | P2 | 已闭环（第 4 轮，88px / 15.5%） |
| **A7** | **作答中途刷新会回退到已答过的题，第一道未作答的题被"越过去"** | 真实浏览器实测：答到 47/48 时刷新，页面回到第 47 题（脚本 `[3b]` 变红）；`work/debug-resume.py` 请求日志显示前端前进时 PATCH 的 `currentQuestionId` 是**刚答完那一题**，服务端指针一路落后一题，而 `restorePosition()` 却把它当首选落点 | **P1**：断点续答的核心承诺"接着答"失效，用户以为进度丢了；未答题只能靠手动"跳到未答的题"找到 | P1 | 已闭环（第 5 轮，含反向验证） |
| A8 | 注册时的免责声明同意项（原 A4） | 用户 2026-09-17 明确选择"实施" | 告知义务闭环 | P1 | 已闭环（第 6 轮，含契约与 README 同步） |
| A9 | 报告页**没有** AI 分析入口：后端五个端点齐备，前端零消费者 | `frontend/src/` 全文搜 `analyses`/`ai/status` 无命中；`ReportV3View.vue` 无入口。用户 2026-09-17 决定"做"，范围限定报告页生成+展示+失败重试 | 可选能力完全用不上；报告页少一段用户明确要求的视角 | P1 | 已闭环（第 7 轮，真实浏览器 31 项通过） |
| A10 | `scripts/browser-verify-jung-flow.py` 的作答循环偶发停住（复跑时停在 16/48） | 同脚本连跑：一次 FAIL（`/assess/...`，页面 16/48）、一次通过。读按钮状态与真正点击之间隔着一次保存往返，读到"可用"时它可能又变回禁用 | **仅脚本问题**，不影响产品；但会让验收结果忽红忽绿、掩盖真实回归 | P3 | 已闭环（第 7 轮：改成显式等「下一题」恢复可用，修复后连跑 6 次全绿） |
| A11 | `scripts/browser-verify-optimization.py` 的"后端是否就绪"探测**恒为否**：后端明明在跑也一律报 `后端未就绪` 并 SKIP | 实跑输出 `SKIP … （/actuator/health → 0）`，改探 `/api/v3/ai/status` 后变 `→ 401`，改探 `/api/v1/meta` 后变 `→ 200` 却仍 SKIP。三个独立缺陷叠在一起：(1) `vite.config.ts` 只代理 `/api`，`/actuator/health` 落进 SPA 回退拿到 index.html；(2) `browser.new_page()` 停在 `about:blank`，从那里 fetch 一律 `Failed to fetch`；(3) 解析前把响应体截断到 200 字符，截断点正好在 JSON 数组中间，`json.loads` 必然失败 —— 所以**即使后端健康也永远判为不健康** | 这条探测形同虚设：它会掩盖后端真的没起来这件事，让"SKIP"看起来像正常状态 | P2 | 已闭环（第 7 轮：落到站点后探匿名接口 `/api/v1/meta`，解析完整响应体；现在后端就绪时是 PASS 且记录 contentVersion） |
| A12 | 复测比较**整页缺失**，且 `compareReports` 是死代码 | `frontend/src/api/v3Assessment.ts` 里 `compareReports()` 与 `CompareResult` 都已写好，全仓**零消费者**（全文搜 `compareReports` 只有定义处）；`router/index.ts` 无 `/reports/compare` 路由。⚠️ 复核时纠正了 backlog 原先的说法：接口消费者不是"完全没有"，而是"有客户端、没有页面" | 后端能力白做；已写好的客户端代码无人调用，将来会被当成"没实现"再写一遍 | P2 | 已闭环（第 8 轮，真实浏览器 28 项通过） |
| A13 | 管理后台 AI 设置页整页缺失，`AdminController` 的四个端点在前端**零消费者** | 全文搜 `/admin/ai-settings`、`fetchAdminUsers` 只命中后端与契约；`router/index.ts` 无 `/admin` 路由；`AccountView` 无入口 | 后台能力只能靠环境变量 + 重启使用；`apiKey` 轮换要动部署配置 | P1 | 已闭环（第 9 轮，真实浏览器 31 项通过） |
| A14 | 后台**表单数字字段填不进值**（`readPositive` 直接 `.trim()`） | 新增 `AdminView` 的第一版：`type="number"` 的输入框在真实浏览器与 jsdom 里都会把 `v-model` 值算成 **number**，于是 `raw.trim is not a function` 抛出，被 `catch` 吞成"保存失败"。是 `adminView.spec.ts` 里"把 maxTokens 改成 2000"这条普通路径先撞出来的；`vue-tsc` 看不出来（字段声明为 string） | 管理员改任何一个配额都会看到"保存失败"，且原因被错误提示掩盖 | P1 | 已闭环（第 9 轮：`readPositive` 按运行时事实收 `unknown` 并先 `String(...)`） |
| A15 | **管理员在后台保存的 apiKey，AI 模块永远读不到**：`SecretCipher` 只有接口、没有实现 | `com.typeme.ai.port.SecretCipher` 全仓仅接口定义，无实现类、无测试（`grep SecretCipher backend/src` 只命中接口与注入点）；后台写入侧 `AiSettingsService` 用 `TextEncryptor`（`Encryptors.delux` + `typeme.security.settings-secret` 派生盐）加密落库，读取侧 `AiRuntimeSettingsProvider` 解密只能靠注入该 port，拿不到就回退环境变量并只打一条 WARN。复现：`AdminAiKeyReachesAiModuleIT` 走真实写入+真实读取路径，修复前 `Tests run: 3, Failures: 2`（bean 为 null；`hasApiKey()` 为 false） | 管理员填完 key、页面显示"已保存"、指纹也变了，但创建分析返回 503 `AI_NOT_CONFIGURED`；**用户按提示做完了每一件事，系统却说他没配**。两个模块各自的测试都是绿的，所以单边测试永远发现不了 | P1 | 已闭环（第 10 轮：新增 `security/SecretCipherConfig` 提供实现；真实调用 PASS 30/0） |
| A16 | 后台**保存设置后 10 秒内仍按旧配置跑**：`AiRuntimeSettingsProvider.invalidate()` 存在但无人调用 | `invalidate()` 只改 `cachedAt`，全仓无调用方；生效设置缓存 `CACHE_TTL = Duration.ofSeconds(10)`，写入方 `AiSettingsService.update()` 保存后不通知读取侧 | 把「演示模式」关掉保存、立刻点生成分析，仍按 mock 配置跑 —— 不报错，只给出"看起来正常但用的是上一版配置"的结果。这类静默错配比报错难发现得多 | P2 | 已闭环（第 10 轮：新增 `AiSettingsCacheInvalidator` port 避免 account→ai→account 环路；保存后立即失效；用例先灌缓存再改、中间不 sleep） |
| A17 | `browser-verify-ai-analysis.py` 的 `result.json.**aiMock` 恒为 `true`，与实测相反 | `finally` 分支写死 `report(None, username, True, True)`；真实模式下观察记录写着 `mock=False`、断言也走真实分支，但产物字段是 `True` | 任何依据该字段判断"这份结果是不是演示数据"的人都会被误导；真实调用的证据会被记成"演示数据"。另外 `mock` 专属断言（必须出现「演示数据」标注）在真实模式下必然 FAIL，使这条脚本无法用于真实验收 | P2 | 已闭环（第 10 轮：改用 `/ai/status` 实测值；mock 断言按模式改判；已更正本次产物并说明原因） |
| A18 | 四个**会真实落库**的 AI 错误码在前端没有专门说法，落进万能兜底：`UPSTREAM_401` / `UPSTREAM_402` / `UPSTREAM_429` / `UPSTREAM_5XX` / `UPSTREAM_UNAVAILABLE` / `UPSTREAM_ERROR` 只覆盖了最后一个 | 后端 `DeepSeekException.Codes` 声明了 12 个任务失败码，前端 `aiFailureHint` 的 `switch` 只 `case` 了 8 个，其余走 `default`。两侧都"有测试"：后端用例只断言"库里存了这个码"，前端 `default` 分支也会成功给出一句话 | **管理员 key 失效（401）或余额不足（402）时，用户看到的是"这次生成没有成功，可以重试一次"** —— 而重试一万次也不会成功，真正该做的是换 key / 充值。用户会一直点重试，问题永远不暴露给能修它的人 | P1 | 已闭环（第 11 轮：补 6 个分支；401/402 明说"需要站点管理员处理，你自己重试没有用"） |
| A19 | 上述漂移**没有任何机制守着**：后端新增一个错误码，不会让任何测试变红 | 无跨端契约测试；Java 与 TypeScript 之间没有共享常量，两侧的码清单只能靠人眼比对 | 这类漂移天生单边测不出来（见 A18），下一次新增码会原样重演 | P2 | 已闭环（第 11 轮：`AiErrorCodeContractTest` 直接读 `frontend/src/api/v3Ai.ts` 的 `case` 分支机械对账；先删一个 case 验证它会红，再恢复） |
| A20 | 跨设备冲突横幅**只写"有改动没写上去"，不说丢的是哪一题、自己选了什么** | `ConflictState.pendingQuestionIds` 的注释写着"（重新载入后可以对照查看）"，但全仓**只写不读**（`grep pendingQuestionIds` 只有定义处与赋值处）。`reloadLatest()` → `reload()` → `applyDetail()` 会整体替换 `this.answers`，本地那条未写上去的作答就此消失 | 用户看到"本机刚才的改动没有写上去"却无从核对是哪一条；点一下「载入最新进度」后连自己刚才选过什么都不确定，无法在载入后把那题补回来。**不是静默丢，但是"不可核对地丢"** | P1 | 已闭环（第 12 轮：新增 `ConflictState.lostAnswers`，横幅逐条列出「第 N 题 · 档位文案」；真实浏览器双上下文制造 409 验证 22 项通过） |
| A21 | 五档文案 `SCALE_CAPTIONS` 只存在于 `AssessView.vue`，store 若也要用就会各写一份 | 冲突横幅要显示"用户当时选的那一档"，文案来源必须在 store 与页面之间共享 | 两代产品各说一套话（`ANSWER_CAPTIONS` vs `SCALE_CAPTIONS`）是历史上已经踩过的坑；同代再分叉一次会以同样方式出问题 | P3 | 已闭环（第 12 轮：提到 store 作为唯一来源，`AssessView` 改为 import） |
| A22 | `app_user_session` **没有批量过期清理**：过期后再未回访的会话行永久残留 | 删行只发生在注销（`deleteByUserId`）、禁用、改密码（`deleteBySessionId` / `deleteByUserIdExcept`）以及"已过期会话再访问一次"时（`SessionAbsoluteTtlFilter:88-93` 把删除当副作用）。容器侧闲置超时（`server.servlet.session.timeout=2h`）**不碰这张表**；全仓无清理型 `@Scheduled`（只有 `AnalysisWorker` 与 `AccountDeletionService` 两个） | 表会随"注册一次就不再回来"的账号无界增长；`SessionAbsoluteTtlFilter` 每个已认证请求都要按 `session_id` 主键查一次这张表，行数越大占用越多 | P3 | 用户 2026-09-17 选择"收进 backlog 先不动"：当前量级下影响可忽略，且清理策略（定期任务 vs 惰性删除）需要单独决策 |
| A23 | **Redis 作为后续架构项**（用户 2026-09-17 明确选择保留） | 现状：会话走容器 `HttpSession` + `app_user_session` 绝对期限账本（每请求 1 次主键 SELECT，`SessionAbsoluteTtlFilter:86`）；`SessionRegistryService` 用**进程内** `ConcurrentHashMap`；限流/额度用 MySQL 事务；AI 任务队列用 DB lease + 2s 轮询。**Redis 在本版没有落点**：单体单实例，"跨实例共享会话"这个最大卖点用不上；而上多实例时它会真正成为必需 | 若将来水平扩展，需要 Redis 的三处：① 会话（替换进程内 registry）② 限流/额度（注意：这里是**一致性**而非性能问题，切换要先定"以谁为准"）③ AI 任务队列（替换 DB lease）。本版不动代码 | P3 | 仅记录（架构评估，未实施） |
| A24 | **是否引入 LangChain4j**（用户 2026-09-17 询问） | 核实结论：**不建议**。① 开发提示词第 40 行是硬边界"不引入独立 AI 框架"；② 现有 `HttpDeepSeekClient`（约 200 行）已承载领域语义：`thinking.type=disabled`、`response_format=json_object`、不跟随重定向（避免把 `Authorization` 带去非预期主机）、401/402/429/5xx 分码、`Retry-After`（秒数与 HTTP-date 两种形态）、以及**缓存命中/未命中 token 分开**（`prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`）—— 后者正是 `AiCostEstimator` 的计价输入（`AiCostEstimator:42-44`）。LangChain4j 的 `TokenUsage` 在部分 provider 上缺缓存 token 字段（见 [issue #4067](https://github.com/langchain4j/langchain4j/issues/4067)、[PR #4080](https://github.com/langchain4j/langchain4j/pull/4080)），换过去可能**丢**这个能力；③ 框架主要省的是"模型适配 + tools/RAG/memory"，而本项目不用 tools、知识来自固定内容包、记忆不需要 —— **省的是用不上的那部分**，要付的是新依赖 + 又一次框架迁移 | 若仍要推进：做限时 PoC，验收口径是"不丢缓存 token 计价、不丢 401/402/429 分码与 Retry-After、不丢 幂等/预算/lease"，并保留回退 | 待用户决定（默认：不引入） |
| A25 | **发给 AI 的每条证据都在撒谎**：`contribution()` 因缺字段静默返回 0，导致每题位置都被写成"选了中间（两边差不多）" | `packageQuestions()` 重建题目节点时只放 `id/scenario/leftPole/rightPole/order`（注释写明"只带最小必要字段"，因为已按维度分组），而 `contribution(JsonNode, int)` 却从节点读 `dimension`，读到 null 即 `return 0`。后果有三层：① 每条证据文本都是"中间档"，模型读到的是**伪造的位置**（rating=5 也写"两边差不多"）；② `bestSameDirection`（只认 \|c\|≥1）与 `strongestOpposite`（显式跳过 c==0）**永远返回 null**；③ 补足名额的 `|c| 降序` 退化成按 `order` 升序 —— 即"证据"变成**按题号取前 8 条**，与答题内容无关。**实测对比**（同一夹具）：缺陷版 `EI-01…EI-08` 全是"中间"；修复版为 `EI-01(c=2 很靠右) / EI-11(c=-1 反向) / EI-05(c=0 真的中立)`。它长期绿着的原因：既有测试对证据文本**只有一条否定断言**（`AnalysisFlowTest:574` 断言不含题干），**没有任何一条检查位置文案** | P1（AI 输入质量，非页面崩溃） | 已闭环（第 14 轮：`contribution` 改为显式接收维度；边界维度补一条"真的中立"证据；新增 5 条测试并逐条验证先红后绿） |
| A26 | 提示词文件与 `prompt-version` 的一致性**只靠人眼** | `SystemPrompt.load` 按 `ai/prompts/<version>.txt` 找文件、缺失即抛；文件第一行的版本标记"便于人工核对"。改文件名、改第一行、或配置默认值写成不存在的版本，都只在**真正发起分析**（用户正在等结果）时才暴露 | P3 | 已闭环（第 14 轮：`PromptFileContractTest` 3 条断言；已用"改坏第一行"证明它会红） |
| A27 | `AiAnalysisPanel` 依赖一个**隐式**前提才不串台 | 面板只在 `onMounted` 里 `ai.loadJobs(props.reportId)`，**没有** `watch(() => props.reportId)`；父组件用 `v-if="reportId"` 挂载它。之所以现在正确，是因为报告正文包在 `<template v-else-if="view">` 里、换报告时 `reports.loading` 置位导致整块**销毁重建**。第 14 轮用真实浏览器（`browser-verify-history-ai.py`，PASS 18/0）证实当前**不串台**，但这条保护是隐式的：若有人把加载态改成"保留旧内容 + 局部骨架屏"（常见体验优化），面板就会开始显示**上一份报告**的分析，而且 `onBeforeUnmount` 的 `stopPolling` 也不会触发（后台会继续每 3 秒轮询）。**注意**：本轮初版脚本只数 `data-ai-job` 的个数，**那个断言抓不住这个 bug**（"1 个任务"无法区分是哪一份的）；已补 `data-ai-job-id` 并改为对比 jobId | P2（当前行为正确，但是**脆的**） | **已闭环（第 17 轮）**：不再依赖"父组件会销毁重建"这个隐式前提 —— `AiAnalysisPanel` 新增 `watch(() => props.reportId)`（换报告即 `reset()` + 重新读任务）、`onBeforeUnmount` 统一 `reset()`；store 增加 `reportId` 归属：`ownJobs` 只认 `job.reportId === state.reportId`，`loadJobs` 带 `loadToken` + 归属过滤，`startPolling` 前置条件 `reportId === null || !hasRunning` 直接 `stopPolling()`。判别力已验证：去掉 `loadJobs` 的 token/归属守卫精确红 2 条、去掉 `startPolling` 前置条件精确红 1 条 |
| A28 | **导出把失败伪装成"你没有这部分数据"**：`safeQuery` 吞掉一切 `DataAccessException` 返回空数组，响应无任何标记 | 账号页在导出成功时说「报告与 AI 分析记录**都在里面**」，同页注销区写着「注销会删除你的全部测评记录与报告，无法恢复……导出数据是**唯一**能把它们带走的办法」。于是报告查询一旦失败（缺列、连接抖动），用户拿到一份**看起来完整**的备份、按指引注销，没导出到的部分**永久丢失且事后无从发现**。日志也只记异常类名，连哪一段失败都看不出来 | **高** | **已闭环（第 15 轮）**：响应新增 `degradedSections: [{section, reason}]`；缺表（部署未就绪）与查询失败分开报且都用段名；前端非空时不说"都在里面"，改说"不是完整备份，先不要注销"，并在注销区重复。证据 `verification/2026-09-17-export-degraded/`（PASS 19/0，含文件里确有该字段） |
| A29 | **保存失败被下一次成功掩盖 → 作答静默丢失**：`flush()` 每次只发当前一条，失败只置 `saveState='error'`，下次成功**无条件**置回 `'saved'` | 场景：答 Q10 时 PATCH 超时 → 继续答 Q11 成功 → 界面显示「已保存」，而 Q10 在服务端从未存在，刷新后回到未作答。且 `assessment.lastError` 在整个 `AssessView` 里**从未被渲染**，用户只看到「未同步（网络或登录已失效）」，连真实原因都丢掉 | **高** | **已闭环（第 15 轮）**：store 新增 `unconfirmed`（本地改了、服务端未确认的作答），下次写入一并重发（服务端 upsert 无副作用），集合非空时不允许回到 `'saved'`；页面新增「未同步（N 题的作答还没写上去）」+ 真实原因 + requestId + **重试保存**按钮。判别力已验证：去掉重发逻辑精确红 2 条 |
| A30 | **NEEDS_REVIEW 是一页死路**：`jumpToFirstUnanswered()` 在 `unansweredBaseIds` 为空时**静默返回** | 而 `finishStage()` 只在本地没有未答题时才调 `runReview()`，所以走进 needs-review 时"未答清单"通常必然是空的 → 点「回到未答的题」零反应，用户只能刷新页面才出得来。`insufficientDetails()` 其实已经算出是哪一维短了 | **高** | **已闭环（第 15 轮）**：按"用户能做什么"排优先级（真有未答题→去那一题；否则去覆盖不足那一维的第一道主测题），并据情况把文案改成「回到题目继续调整」。判别力已验证：恢复早期 return 精确红 1 条 |
| A31 | **进入补充题后无法返回主测**：跳过按钮只在 `clarify-offer` 那一步存在 | 契约把「跳过补充题」写成用户可选的动作，但一旦点了「开始补充题」就只能把补充题全部答完；刷新也会被 `restorePosition()` 重新落回补充阶段 | 中 | **已闭环（第 15 轮）**：补充阶段保留「回到主测改答」与「跳过补充题，直接交卷」两个出口 |
| A32 | **会话失效时答题页无回路**：只给「请先登录」+一个必然失败的「重试」 | 答题页在 `App.vue` 里隐藏了常规导航（`quizActive`），登录入口只在 `v-else` 分支渲染，所以页面上**没有任何能走通的操作**；`AssessView` 也没用过 `isSessionExpired` | 中高 | **已闭环（第 15 轮）**：识别 `UNAUTHENTICATED`，给出「去登录，然后接着答」（路由守卫本就支持 `redirect`），并不再为这种情况提供「重试」 |
| A33 | `AttemptService.patchAnswers` 的冲突检测**比正确做法弱**：丢弃 UPDATE 受影响行数，改用"另查一次 `COUNT(*) WHERE revision = 新版本号" | 那个 COUNT 只能证明**有人**把 revision 推到了该值 —— 真并发时另一个请求的提交也会被数进去，于是"自己一行都没改到"会被判成 200。正确做法是用 UPDATE 的受影响行数。**注意**：我为此写的串行测试**通过了**，反证了我最初"串行即可复现"的推断是错的；真并发窗口在 H2 + MockMvc 下无法确定性构造 | 中（真并发下才显现） | **已闭环（前一会话 review-fixes 改动；第 28 轮复核代码与测试确认）**：`AttemptService.patchAnswers` 现在用 `int affected = jdbc.update(...)` 并在 `affected != 1` 时抛 409（`AttemptService.java` 约 L419/L434），不再用 `COUNT(*)`。回归测试 `jung/api/AnswerRevisionConflictIT#staleRevisionIsRejectedEvenWhenAnotherRequestAlreadyBumpedIt`（H2，**不需要真并发**：第二次调用读到的 `currentRevision` 已被上一次推高，`newRevision` 恰等于库里现值，`COUNT` 必然 > 0 —— 旧写法会把它掩盖成 200）。该用例在后端全量子集中实测通过。原先“建议将来用真 MySQL 并发测试证实后再动”的前提已不再成立（改动已做，且不需要真并发就能判别）。 |
| A34 | `ReportService` 在覆盖检查**之前**就写 `clarification_skipped = 1`（第 33 轮复核：写入**与草稿是否进入澄清阶段无关**，只要一次带跳过标记的提交覆盖不足就会落库，可达性比原记录高、级别由「低」上调为「中」） | 原记录写的是「要撞上需要覆盖率在 review 与 submit 之间由 ok 变回不 ok」，据此标成「不可达」，但实际路径更短：带 `clarificationSkipped: true` 且覆盖不足 → 写库 1 并返回 200 + NEEDS_REVIEW（**没有报告**）；草稿仍在 BASE_IN_PROGRESS，之后补答主测题**不会**触发 `patchAnswers` 里的 `resetClarification`（那条只在 CLARIFICATION_IN_PROGRESS 且改主测答案时才清标记），于是下次老实答完补充题交卷会被 `400 已选择跳过补充题，就不应该再有补充题答案` 顶回，而那次提交没有报告、也无法派生新测评绕开 → 草稿锁死 | 用户点过一次「跳过补充题」又回头老实答题，就再也交不了卷 | **已闭环（第 33 轮）**：落库推迟到「确定要写报告」之后（报告 INSERT 成功之后、`status=SUBMITTED` 之前，同一事务）。`SubmitReportIT#skipChoiceIsNotPersistedWhenTheSubmitProducesNoReport` 走「覆盖不足+跳过 → NEEDS_REVIEW → 补答 → 求澄清 → 答完补充题 → 再交卷 201」完整路径；判别力：把落库改回原位置 → 1 failed / 2 passed（`Expecting value to be false but was true`）；后端全量 407/0/0/1。证据 `docs/optimization/verification/2026-09-22-a34-a40/` |
| A35 | `POST /attempts` 未兑现 `Idempotency-Key`，前端 `createKey` 是**死字段** | 契约 02 §7.2 写"可选 Idempotency-Key"，`JungController` 没有 `@RequestHeader`；`assessmentV3.ts` 的 `createKey` 只被置 null，从不写入/读取。create 丢响应后点「重试」会新建第二份草稿。当前前端没有任何 `fetchAttempts` 调用点、没有草稿列表，所以用户暂时看不到 | 低（用户不可感知） | **已闭环（第 18 轮，授权变更后）**：新增 `jung/service/IdempotencyGuard.java`（`api_idempotency` 的唯一读写点：`claim`/`complete`/`release`/`forgetDangling`/`completedRef`，含"死掉的 `IN_PROGRESS` 超过 2 分钟可被重新占用"这条逃生口）；`JungController.createAttempt` 接 `@RequestHeader("Idempotency-Key")`；`AttemptService.create` 拆成"占用 → `createOnce`（事务）→ 标记完成"三段，任一段失败都不影响已创建的草稿；`assessmentV3.create()` 自己生成并保存键（重试复用、成功后更换）。判别力/证据：新增 `AttemptIdempotencyIT` 8 条（同键重放同 id、异内容 409 `IDEMPOTENCY_KEY_REUSED`、无键对照、空白键、超长键 400、草稿被删后同键可重建、按用户隔离、过期占用放行）——**其中"草稿被删后同键可重建"一条当场抓出了我实现里的真实缺陷**（`release` 只删 `IN_PROGRESS`，`COMPLETED` 的悬空记录会让这个键永久命中一个 404 的 id）；真实浏览器另有一条：同一个 `POST /attempts` 用 `route.fetch()` 真发两次 → 同一份草稿、草稿总数只 +1 |
| A36 | **恢复码重新生成：201 但响应里没有码时页面零反馈，而旧码已作废** | `readStringList` 把字段缺失读成"正常空数组"；`AccountView` 的恢复码区只在 `recoveryCodes.length` 时渲染，密码框还会被清空。而服务端在返回 201 前已经 `revokeAllUsable` + `bumpRecoveryCodeVersion` —— 用户看不出发生过任何事，也不会去抄新码，等于无提示地丢掉全部恢复码。注册页对**同一个字段**有显式处理（`codesMissing`），这里没有 | 中（需响应缺字段才触发） | **已闭环（第 15 轮）**：新增 `readRecoveryCodes(value, required)`。重新生成时 `required=true` —— 码为空就抛 `UNEXPECTED_RESPONSE`，文案说清"旧码可能已作废、请重新生成并当场抄下来"；注册时 `required=false` —— 账号已经建好，因缺字段把注册判成失败会让用户去重试一个已被占用的用户名，所以照样进下一步、由页面的 `codesMissing` 另行提示。两处期望**故意相反**，`v3RecoveryCodes.spec.ts` 把两边都钉住（判别力已验证：去掉守卫精确红 3 条、注册那条仍绿） |
| A37 | **注销清理吞掉真实数据库异常并把任务标记为 DONE** | `deleteUserScoped` 把所有 `DataAccessException` 吞成 WARN，`cleanup()` 七步后无条件 `return true`，于是 `AccountDeletionService` 必然 `markDone`。清理中途一次真实 DB 失败 → 数据不删、任务却 DONE 且不再重试；此时账号已 DISABLED、用户名已改写，用户**再也登录不进去也无法重发**，而页面承诺的是"正在被删除" | 中（条件触发，但后果是个人数据永久留存） | **已闭环（第 17 轮）**：`cleanup()` 改成七步 `runStep(failed, section, …)`，任一段失败即抛 `AccountCleanupIncompleteException`（消息只带**段名**，不带 PII），`markDeletedWithRewrittenUsername` 只在全部成功后才调用；`tableExists` 只捕 `BadSqlGrammarException`（"表还不存在"= 部署未就绪），其余 `DataAccessException` 一律上抛；`cancelActiveAiJobs` 不再吞异常。新增 5 条 `AccountDataDeletionServiceTest` + 2 条 `AccountDeletionWorkerTest`（失败 → `markFailed("CLEANUP_FAILED")` 且不 `markDone`）；判别力已验证：把 `tableExists` 改回 `catch (DataAccessException)` 精确红 1 条 |
| A38 | 账号页丢弃 `serverMessage`：改密/生成恢复码/注销输错当前密码时显示「用户名或密码不对」 | `INVALID_CREDENTIALS` 的映射文案是给登录页写的，而这三个表单里根本没有用户名字段；后端给的精确说法"当前密码不正确。"被这一页丢掉（登录/注册/恢复三页都渲染了"服务器说明"） | 中低 | **已闭环（第 19 轮）**：抽 `frontend/src/components/FormErrorNotice.vue`（主文案 + 字段提示 + 服务器说明 + 限流秒数 + 报障编号的**一处定义**），账号页五个表单（昵称/密码/恢复码/导出/注销）全部改用它 —— 少一个字段应当是编译错误，而不是某个页面上少一句准确的话。新增 `frontend/src/components/formErrorNotice.spec.ts` 5 条钉字段集合；真实浏览器实测：输错当前密码后 `[data-account-error-password]` 里同时出现主文案与「服务器说明：当前密码不正确。」（`verification/2026-09-18-account-page/`） |
| A39 | 账号页六个区块里只有「修改密码」是主色按钮，其余都是次级按钮 | 第 16 轮视觉对齐：`AccountView.vue` 的按钮层级由子代理保留原样（它被授权只做布局）。这一页本身没有单一主行动，所以保留层级是合理的；但"一页一个主色按钮"是本站其它页面的口径 | 低 | **已闭环（第 19 轮，按"没有单一主行动 → 零主色"处理）**：这一页是平权的六个设置区块（昵称、密码、恢复码、导出、注销、自我理解），主色暗示"这是推荐动作"没有依据；`修改密码` 改回 `btn-secondary`，层级交给区块标题。判定用可测量的东西：页面里 `.btn-primary` 数量为 **0**、危险按钮是 `btn-danger` 且背景色与次级按钮不同、未点开面板时页面里**不存在**「永久删除」按钮。证据同上（真实浏览器 PASS 32/0/0） |
| A59 | **`FormErrorNotice` 的「报障编号怎么用」那句话在所有页面都没渲染过**（第 19 轮新发现） | `requestHint` 是布尔 prop，而 Vue 会把**缺席的布尔 prop 转成 `false`**（`disabled` 那套语义），于是模板里 `v-if="requestHint !== false"` 永远不成立 —— 组件注释写的是"默认显示"，实际从来没显示过；A38 修好"服务器说明"之后，同一块里这行仍缺失 | 中低（用户拿到编号却不知道要拿它干什么；报障效率直接受影响） | **已闭环（第 19 轮）**：改用 `withDefaults(defineProps<…>(), { requestHint: true })`，让默认值由类型系统保证而不是模板条件。判别力：修复前的 5 条新测试里**精确红了这 1 条**（"反馈问题时把这个编号一起发过来"找不到），修后 5/5 通过；真实浏览器也确认这句话在页面上出现（同一条验收里的 A59 断言） |

| A40 | 提示与按钮间距在页面之间不统一（账号页 `mt-3`、对比页 `mt-4`） | 第 16 轮实测：两页在最窄 320 下都无溢出、无遮挡，纯粹是节奏差异 | 极低（无功能影响） | **已闭环（第 33 轮）**：以账号页既有节奏为基准（表单反馈 caption / notice-success / notice-error + 其后按钮都是 `mt-3` = 12px），把对比页操作组与比较失败提示从 `mt-4` 改到 `mt-3`；分节层级间距（mt-5/6/8、卡片）不动。判别力做在**构建产物层**：新脚本 `scripts/browser-verify-spacing.py`（真实 dist + 打桩 API）在 320/390/1440 量两页的 `marginTop` 并断言相等 —— 修复后 32/32，撤掉修复重新构建后 23/32（9 条精确红，全是「对比页 16 vs 账号页 12」，且无任何溢出失败）。证据 `docs/optimization/verification/2026-09-22-a34-a40/` |

## 第 17 轮新增（2026-09-18 深夜）

> 这一轮的起点是"AI 分析与业务链路的**真实性**"：把"界面看起来对"与"服务端事实"分开核对。
> 每条都先写出**用户能观察到什么**，再看代码为什么。

| ID | 问题 | 证据（代码位置 / 实测） | 用户影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A41 | **手快连点两档会伪造一场"跨设备冲突"** | `stores/assessmentV3.ts` 的 `flush()` 之间没有串行化：第一发 PATCH 在途时第二次点击会再发一发，两发带**同一个** `expectedRevision`，第二发必然撞服务端严格 CAS → 页面弹出「另一台设备改过这次的进度」并把用户自己刚点的那一题列进"本机改动没有写上去" | 根本没有第二台设备：用户被告知进度被别的设备改了，还被要求"载入最新进度后重答一遍" | P1 | **已闭环（第 17 轮）**：`flush()` 改为无参 + `if (this.flushing) return` 单飞，跑满 3 轮 `sendPending()`；`select()` 先把作答记进 `unconfirmed` 再 `await flush()`，只有响应内容与本地**逐条一致**（`samePatch`）才把它移出未确认集。组件测试 3 条 + 真实浏览器实测（连点两档 → 无横幅、收敛到已保存、刷新后仍是最后点的那一档） |
| A42 | **"自己那次写入其实成功了"会被误判成冲突** | 上一发 PATCH 响应丢在路上（超时/断网）时本地 revision 落后，用户点「重试保存」必然拿到 409，而这条 409 的含义是"你自己已经写进去了" | 用户被推着去"载入最新进度"，把他刚写成功的那一题当成别人的改动丢掉 | P1 | **已闭环（第 17 轮）**：新增 `reconcileAfterConflict()` —— 409 之后**先重读详情逐条比对**，服务端内容与本机一致就按 `已保存` 收敛（`applyDetail` + `saveState='saved'`），只有真的不一致才弹横幅。2 条组件测试（一致 → 无横幅；不一致 → 必须有横幅），去掉这段逻辑精确红 1 条 |
| A43 | **交卷响应丢失后，用户回不到那份已经生成的报告** | 契约里 `attempt.reportId` 就是为这条路准备的，前端**从来没读过**：交卷超时 → 重试只得到「这份测评已经提交」→ 刷新答题页也停在"答完的题"上，唯一出路是"再测一次" | 报告已经存在，用户却看不到，还可能白做一遍 | P1 | **已闭环（第 17 轮）**：新增 `probeSubmission()`（只读、不碰 `lastError`/`saveState`），交卷失败且非会话失效时探一次：已提交且有 reportId → 直接跳报告；已提交但没报告 → 进 `needs-review` 并说明原因。`bootstrap()` 也补了同样判断（刷新后自动打开报告）。真实浏览器用 `route.fetch()+abort` 丢响应实测通过 |
| A44 | **删除报告在途无守卫，失败还会把错误写进"列表"通道** | `reportV3.remove()` 没有在途守卫（连点会发多个 DELETE），失败时写 `listError`；而列表模板的 `v-if="listError"` 会把整个列表换成「记录没能载入」——记录明明好好地在列表里 | 删除一次失败，用户看到的是"你的记录都没了"；连点两下还会发出两个删除请求 | P1 | **已闭环（第 17 轮）**：新增 `removeError` / `removingId`（在途守卫 + 独立错误通道）、`clearRemoveError()`；`ConfirmDialog` 增加 `busy`/`busyLabel`（在途时禁用两个按钮 + `aria-busy` + 文案「正在删除…」）；行内删除按钮在途时禁用。2 条组件测试 + 真实浏览器注入 500 实测（列表行数不变、不出现「记录没能载入」、不出现冲突横幅） |
| A45 | **比较页换掉一份报告后，旧对照表仍在页面上** | `CompareView` 的 `watch([idA, idB])` 缺失（第 17 轮新增），换选只改 URL 查询参数；用户看到的是"表格和选择对不上"，而且表格里没有一处写明"这次比的到底是哪两份" | 用旧结论去理解新选择 —— 一份对照表可以解释成完全不同的一对报告 | P1 | **已闭环（第 17 轮）**：新增 `watch([idA, idB])` 立刻作废旧结果（`generation++`、清 `result`/`resultIds`/`compareError`、`comparing=false`），需要时自动比较；新增 `resultIds` 与 `[data-compare-subject]` 表头写明"这次比的是：先看的那份 X；再看的那份 Y"。2 条组件测试 + 真实浏览器实测（清空第二份 → 表格与表头一起消失；选同一份 → 说明不能比；换回来 → 表头与第一次一致） |
| A46 | **`/reports/{id}` 的 404 被说成"你还没做完"** | `ReportV3View` 里 `reports.notFound` 只有一套文案（"这次测评还没有报告可看…回去把没处理的题补齐"），但这一页**只能按 reportId 取报告**（`loadReportByAttempt` 全仓无调用方），所以 404 的真实含义是"这份报告不在这里" | 删掉一份报告后按浏览器后退，用户被告知"你还没做完"，并被送去重新测一次 —— 既不是事实，也丢掉了真正的下一步 | P2 | **已闭环（第 17 轮）**：store 增加 `loadedByAttempt`，页面按它分两套文案：按 attempt → 保留"信息不足/回去补答"；按 reportId → "这份报告打不开了 + 已经删除 / 属于另一个账号 / 编号不完整"，主行动改为「回到历史报告」。真实浏览器实测 4 条通过 |
| A47 | **管理员探针缓存跨账号，且把 401 缓存成"不是管理员"** | `composables/useAdminProbe.ts` 的 `cached` 是模块级：管理员退出、普通用户在同一页登录后 `cached=true` 仍然生效（后台入口照常显示）；401 分支还会 `cached=false` —— 与文件里"失败不缓存"的注释相反，同一页重新登录后管理员看不到入口 | 共用设备上把后台入口留给下一个登录的人；或让真正的管理员以为入口消失了 | P2 | **已闭环（第 17 轮）**：会话失效不再写缓存（只 `isAdmin.value=false` 并返回），新增 `resetAdminProbe()` 并在 `auth.applyProfile` / `applyAnonymous`（所有身份变化路径的汇聚点）调用。2 条组件测试（401 后重新登录能拿到入口；换账号必须重新问一次） |
| A48 | **载入失败不区分"再试能好"和"再试一万次也不会变"** | 答题页的 `loadFailure` 分支只有「重试」；而 404（测评已不存在）/ 409 `PACKAGE_UNAVAILABLE`（内容包已下线）/ 403（不属于本账号）重试结果永远一样 | 用户对着一个必然失败的按钮反复点，也没有别的出路 | P2 | **已闭环（第 17 轮）**：记录 `loadErrorCode`，新增 `loadUnrecoverable`，这几种失败改成"如实说明原因 + 「重新开始一次测评」为主、「再试一次」为辅"。2 条组件测试 |
| A49 | **会话失效时登录/注册/恢复三个页面之间会丢掉 `redirect`** | `LoginView` 的注册/恢复链接是硬编码路径、`RecoverView.goLogin()` 写死 `redirect:'/account'`：用户从答题页被送到登录页、再走"用恢复码重置"，回来就落到账号页而不是那道题 | 重置密码之后回不到原来的位置 | P3 | **已闭环（第 17 轮）**：两处链接都带上 `route.query`，`RecoverView` 复用同一套站内路径校验（`//evil.example` 仍然被挡） |
| A50 | **保存时 401：答题页给的是必然失败的「重试保存」** | 保存失败的兜底只有重试按钮；`lastError.sessionExpired` 已经有这个信息但页面没用 | 会话失效时用户被按在一个永远失败的按钮上 | P3 | **部分闭环（第 17 轮）**：页面按 `lastError.sessionExpired` 换成「登录后接着答」（带 `redirect`）。**真实浏览器实测发现 App 层的会话失效桥更早生效** —— 用户会被直接送到登录页并带 `redirect` 回到原页，所以这条页面内分支实际只在"没有触发重定向"时兜底；两条路径都已验证（组件测试钉页面内分支，浏览器钉实际跳转与回跳） |
| A51 | `/assess` 每次都新建草稿，首页却承诺"换设备接着答" | 首页文案与 `data-primary-entry` 都直接 `POST /attempts`；`fetchAttempts` 无调用方 → 没有"继续上次未答完"的入口，旧草稿不可见也不可删 | 换设备/换浏览器后用户只能从头再答一遍，服务端却存着一堆没人能看见的草稿 | P2 | **已闭环（第 18 轮）**：`assessmentV3` 新增 `drafts`/`draftsError`/`draftProgress` 与 `loadDraftEntry()`（**只读**，绝不写进答题状态）、`clearDraftEntry()`，以及 getter `resumableDraft`/`otherDraftCount`；`LandingView` 在已登录时读草稿列表，主入口变成「继续上次没答完的测评」并写明「已答 N/M 题（主测）· 上次答到 …」（进度只信真读到的那份详情，读不到就不显示）、次要入口变成「重新开始一次测评」、多份时补一句「另外还有 N 份没答完」；未登录时**不发**该请求并清掉上一个人留下的入口。证据：单测 `landingResume.spec.ts` 8 条（判别力已验证：`resumeTarget` 恒 null → 精确红 5 条；详情失败时编造 `answered:0` → 精确红 1 条）、真实浏览器 12 条（含"点继续后草稿总数不变"） |
| A52 | `AdminView` 把 401 渲染成"可能只是后端暂时没响应"，且没有登录路径 | `AdminView` 的探测失败分支与 403 共用一句话 | 会话过期时管理员看到的是"可能后端没响应"这种猜测，页面也没有入口重新登录 | P3 | **已闭环（第 18 轮）**：`access` 增加 `'needs-login'` 态（`isForbidden` → `denied`，`isSessionExpired` → `needs-login`，其余 `unavailable`），新增独立提示块 `[data-admin-needs-login]`（说"登录状态已经失效"、给 `redirect=/admin` 的登录入口、给报障编号），并把 `unavailable` 的文案改成不再猜原因（明确"权限不足与登录失效都会单独提示"）。判别力已验证：401 回落到 `unavailable` → 精确红 1 条。**真实浏览器实测的收场是"被 App 层会话失效桥送到登录页并带 redirect"**，页面内这一支只有组件测试覆盖（与第 17 轮保存 401 同一情形，已写进验收报告的诚实交代） |
| A53 | 若干**低概率但真实**的后端边界（第 17 轮审查登记，第 19 轮修掉其中四条） | ① `submit` 并发下撞 `uk_report_attempt` → 404「这份报告」；② 读额度失败时 `status()` 按 `used=0` 算 → 界面报一个"满额度"；③ `ReportService.exportData`（无调用方的死代码）仍把 `BadSqlGrammarException` 吞成 `List.of()`；④ submit 返回 200 而契约写 201；⑤ 同一账号并发两次 `DELETE /me` 撞 `uk_deletion_job_user` → 500；⑥ AI 任务 429 自动重试时"重新入队"没有重新预留额度，`reserved_calls` 会漂移 | ①⑤ 在真实 MySQL 上**都复现成了真缺陷**；② 是"编一个数字"，用户按它做决定；④ 与契约不符且此前无测试覆盖；③⑥ 仍未复现 | P3 | **已闭环（第 27 轮）**；第 18 轮更新：②③④⑥ 不受"真实 MySQL 授权"影响，但本轮做的是 A35/A51/A52/A54，这六条**一条都没动**（不假装顺带修过）。①⑤ 需要的真 MySQL 并发测试已获授权（见第 18 轮两个 IT 已跑通），可以构造了。**第 19 轮闭环 ①④⑤ + ②**：① 重复提交分支改 `SELECT ... FOR UPDATE`（当前读）；④ `submit` 建出报告时返回 **201 Created**，覆盖不足仍 200 + NEEDS_REVIEW；⑤ 新增 `DeletionJobRepository.insertPendingOrGetExisting`（冲突时当前读回既有任务，重复申请幂等）；② `status()` 在"未登录"与"读额度失败"两种情况下都返回 `remainingToday = -1`（次数未知），前端据此不显示次数 —— 并修掉 `fetchAiStatus` 把缺失字段当 `0`（"今天用完了"）的默认值。证据 `ConcurrencyMySqlIT`（真实 MySQL，3 条）、`SubmitReportIT`（2 条）、`AnalysisStatusQuotaTest`（3 条，含匿名调用不读额度）、`aiAnalysisPanel.spec.ts` +1（-1 时不显示次数也不禁用按钮）、`AccountDeletionIT` +1；每条都做了判别力验证（改回旧写法即红）。**第 27 轮闭环 ③⑥**：③ 删除无调用方的 `ReportService#exportData`（它把 `BadSqlGrammarException` 静默吞成空数组，与账号模块 `degradedSections` 的降级语义不一致），导出只剩 `account/service/DataExportService` 一处定义，并修正 `JungController` 里"由报告模块复用"的过时注释；⑥ 429 自动重试重新入队时**不再退还预留**（那一次重试还没发出去，退了会让 `reserved_calls` 比实际外发少一次），改由 `discardLateResult(sent=false)` 在"重试始终未发出"时退。回归：`AnalysisFlowTest#rateLimitedRetriesAtMostOnce` +2 条断言（入队后 user/global 各 1、重试再次 429 后归 0），判别力已验证（改回旧写法精确红 1 条）。 |
| A55 | 第 17 轮的验证脚本**第一版把纯文字链接也按 44×44 判**，一次跑出 11 条"失败" | `scripts/browser-verify-round17.py` 初版 `small_targets` 的选择器含 `a[href]`，于是隐藏的「跳到主要内容」(1x1)、品牌链接(92x19) 全部计入 | **不是产品缺陷，是判据不一致**：第 16 轮的 `TARGETS_JS` 只量表单控件与按钮 | P3 | **已闭环（第 17 轮）**：选择器改回与第 16 轮完全一致；被误判的两组数字转为观察记录并登记成 A54。**这次"改判据"本身也写进了验收报告的诚实交代**，因为它同样能把红灯变绿 |
| A54 | **顶栏里有一个可点目标矮于 24px**（原记录写的是"翻看历史时顶栏被压缩"状态） | 第 17 轮最终脚本实测：顶栏里只有**品牌链接**（`aria-label="TypeMe 首页"`）低于 24px —— 320px 下 92x19、390/1440 下 203x22；导航项走 `.btn-sm`，都是 44px。**原记录的 `关于 24x16` / `首页 27x22` 在当前的 `header a[href]` 上量不出来**：第 18 轮在三个宽度、滚动前后各量一次，都没有出现这两个尺寸，且顶栏没有任何滚动监听（`App.vue` 里没有 scroll 状态），所以"压缩态"在现在的代码里并不存在 | 鼠标用户几乎无感；触屏/手抖用户在顶栏点品牌回到首页时命中区偏小 | P3 | **已闭环（第 18 轮）**：品牌链接加 `min-h-6`（24px，WCAG 2.5.8 的最小目标尺寸）并保留 `items-baseline`（换 `items-center` 会把副标题的行内盒重排一遍，实测宽度 203→195），实测 92x24 / 203x24 / 203x24、命中区无重叠；**代价是窄屏顶栏高度 88px → 93px**（1440 不变，仍是 69px），已写进验收报告。原始数字对不上这件事也写进了报告的诚实交代 |
| A55 | 第 17 轮的验证脚本**第一版把纯文字链接也按 44×44 判**，一次跑出 11 条"失败" | `scripts/browser-verify-round17.py` 初版 `small_targets` 的选择器含 `a[href]`，于是隐藏的「跳到主要内容」(1x1)、品牌链接(92x19) 全部计入 | **不是产品缺陷，是判据不一致**：第 16 轮的 `TARGETS_JS` 只量表单控件与按钮 | P3 | **已闭环（第 17 轮）**：选择器改回与第 16 轮完全一致；被误判的两组数字转为观察记录并登记成 A54。**这次"改判据"本身也写进了验收报告的诚实交代**，因为它同样能把红灯变绿 |
| A56 | 第 18 轮验证脚本的 `go()` 用 `page.goto('#/some-hash')` 跳到**与当前相同的 hash** 时不再触发导航，随后的断言会读到上一页的 DOM | 浏览器的同文档导航不产生新的 load 事件，Playwright 的 `goto` 立即返回，`wait_for_selector` 命中的是残留节点 | **不是产品缺陷，是验证脚本的假失败**（但反过来也可能造成假通过） | P3 | **已闭环（第 18 轮）**：脚本在跳转前先把 `location.hash` 归零再 `goto`；同类"改判据"动作同样写进了验收报告的诚实交代 |
| A57 | `api_idempotency` **只增不减**：每来一次建测评/交卷/建分析就多一行，TTL 24 小时，而在此之前只有**账号注销**会删（按用户删全部） | 全仓库 `backend/src/main` 里对这张表的 `DELETE` 只有注销清理里那一句；`idx_idempotency_expires` 建好了却没有任何消费者 | 不注销的活跃账号会让这张表无限增长（慢，但方向上是无界的） | P3 | **已闭环（第 19 轮）**：`IdempotencyGuard.deleteExpired(now, limit)` + `IdempotencyCleanupJob`（`@Scheduled`，默认 1 小时一次、启动后 10 分钟首跑、每轮最多 500 行；异常只记日志，不让一次失败停掉后续调度）。分两步"先查主键再按主键删"是因为 `DELETE ... LIMIT` 是 MySQL 专有语法、H2 会报语法错；删除时再带一次 `expires_at < ?`，避免删掉"查到之后又被同 key 并发重新占用"的活记录。测试 `AttemptIdempotencyIT#deleteExpiredOnlyRemovesExpiredRows`（过期/活着/别人的三条记录，只删过期那条），判别力已验证并撤回 |
| A58 | 契约 §7 把 `GET /catalog/*` 归在“公开内容 GET”（不需要会话），实现里 `/api/v3/**` **一律要认证** | `SecurityConfig` 的 `permitAll` 只有 `auth/csrf`、`auth/register`、`auth/login`、`auth/recover` 四条；写 `SubmitReportIT` 时不带会话请求 `GET /api/v3/catalog/current/package` 拿到 **401** | 未登录访客拿不到服务端内容，首页只能退到内置口径（用户看得到内容，但看不到内容包更新）；对已有流程没有功能性破坏 | P3 | **已闭环（第 29 轮，2026-09-21，用户“按你的建议来”授权）**：`SecurityConfig` 对 `GET /api/v3/catalog/current`、`/catalog/current/package` 放开 `permitAll`，并配匿名限流 `OP_CATALOG`（`typeme.ratelimit.catalog`，默认 `5m/120`/IP，**已登录用户不计数**，理由：公共壳每页读一次目录，计入会让正常浏览把自己挡在 429）。新增 `SecurityBoundaryIT#anonymousCanReadCatalog`（200 + ETag == 摘要 `sha256` + 不泄露 `questions`）与 `#anonymousCatalogRelaxationDoesNotOpenUserData`（`/me`、`/attempts`、`/reports`、`/admin`、`/platform/*` 仍全 401），`RateLimitIT#anonymousCatalogReadsAreRateLimited` + `#authenticatedCatalogReadsAreNotInAnonymousBucket`。判别力三条验证（撤回匿名单例判定 → 只红匿名限流那条；撤回两条 `permitAll` → 只红匿名可读那条；把 `/api/v3/**` 放宽成 `permitAll` → 只红用户数据护栏那条）。回归：后端全量 **404/0/0/1**，前端 50 文件/1038 条。证据 `verification/2026-09-21-a58-public-catalog/`。**未跑真实浏览器**（“未登录首页真能显示服务端口径”只有 H2+MockMvc 证据） |


## 第 20 轮新增（多量表平台化）

| ID | 问题 | 证据 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A60 | **平台层前端一条请求都发不出去**：`api/platformV3.ts` 的 `BASE = '/api/v3/platform'`，而 `v3Request` 自己会再加 `/api/v3` | 新增 `assessChooserView.spec.ts` 第一次跑就打出真实请求 URL：`/api/v3/api/v3/platform/instruments`、`/api/v3/api/v3/platform/attempts?page=0&size=50` —— 两条都 404 | 目录永远为空、草稿永远没有、报告永远打不开；而界面呈现的是空态或"服务端还没准备好"，容易被当成后端没部署，而不是客户端拼错路径 | P0 | **已闭环（第 20 轮）**：`BASE` 改成相对前缀 `/platform`，并把原因写进注释。**这条是被新测试撞出来的**：在此之前平台层的前端代码从未真正跑过 |
| A61 | **大五报告一份都生成不出来**：禁用措辞表里有「高于」「低于」，而 `readingOf()` 必然写出"比中间高/低了 N 分" | 端到端 `BigFivePlatformIT#fullFlowProducesBigFiveReport` 第一次跑：交卷 **500**，日志 `Resolved [java.lang.IllegalStateException: 大五报告文案出现禁止措辞「…」…]`。第二层：`limitations()` 的"本产品没有本地常模，因此**不提供百分位**……"里「百分位」也在禁用表内 | 任何一份答完的大五报告都在构造阶段抛异常 → 用户看到 500；而实现级测试（直接构造文本）永远走不到这条路径 | P0 | **已闭环（第 20 轮）**：从禁用表移除「高于」「低于」（保留「常模」「百分位」「低于全国」——要拦的是"与外部参照比较"），并把这句限制声明改写成不含禁用词的说法（**不给护栏开口子**）；护栏报错信息补上来源（"首屏摘要"/"维度解读 ES"/"限制声明"），否则这个异常在 HTTP 上只有一句 500 |
| A62 | **报告详情接口 100% 失败**：`PlatformQueryService` 用 `(String) row.get("created_at")` 强转 `DATETIME` 列 | JDBC 返回 `java.sql.Timestamp`，强转必然抛 `ClassCastException`（`BigFivePlatformIT` 两条用例同时红，就是这一个根因） | 报告能建出来但**永远打不开**；`TimeSource.isoFromUtc(...)` 那条分支从来没被执行过，所以"看起来也对" | P0 | **已闭环（第 20 轮）**：时间一律走 `TimeSource` 转换、不做强转；已用正则复查全仓 `_at` 列的同类写法，只此一处 |
| A63 | 历史问题：大五报告请求 AI 解读曾因缺 EI 维返回 500 | 当前复核：`ReportInputBuilder.build` 先分流 v3，`ReadableReportInput.build` 投影 E/A/C/ES/O、类型为 null；`AiAnalysisPanel.vue` 按提示词版本启用 | v3 支持大五；旧提示词下界面解释暂不支持，服务端保留明确拒绝 | P2 | **第 21 轮已实现**：仅旧提示词路径保留 400 `UNSUPPORTED_INSTRUMENT`。2026-09-18 本次已核对代码和对应测试源，未重跑测试或真实 AI；不据此宣称部署环境已启用或语言质量已验证。见 [裁决复核](../handoffs/2026-09-18/open-decisions-review.md) |
| A64 | 前端报告列表/发现页面对**不存在的量表 slug** 时指向不存在的路由 | `InstrumentsView` 的"查看并开始"原来跳 `{name:'instrument-detail'}`（仓库里没有这个路由，只有 `/instruments` 与 `/instruments/:slug/method`）；另一处 `RouterLink to="/instruments/{slug}"` 同样是死路径 | SPA 回退成空白页，而"开始测评"是这一页最主要的动作 | P2 | **已闭环（第 20 轮）**：按钮改跳 `/assess?instrument=<slug>`（未登录先登录并带 `redirect`），"先看题目与口径"改跳 `instrument-method`；选择页新增 `?instrument=` 自动开始，并对未知 slug 给出明确说法（而不是毫无反应） |
| A65 | 选择页卡片上的"开始"会**再建一份草稿**，而列表区已经有"继续" | `AssessChooserView.start()` 无条件 `POST`；同一页的草稿区却提供"继续" | 用户答过的题留在旧草稿里，"我上次答到哪了"出现两个答案；不报错、界面正常，因此很难被发现 | P2 | **已闭环（第 20 轮）**：`start()` 先查未答完的草稿，命中则继续那一份；`?instrument=` 自动开始还要等"目录已载入 + 草稿已问过一遍"两个条件都落定（早一步就会走成未知 slug 或抢着建新草稿） |
| A66 | 多量表平台方案（`docs/2026-09-18-platform-plan/`）此前**只有后端与方案**，前端零消费者 | 本轮开始时 `frontend/src/` 里搜 `/api/v3/platform` 零命中；路由只有 `/assess` 与 `/reports/:reportId`；平台层没有任何端到端测试 | 两项测评里只有十六型能用；大五的计分、报告、草稿在界面上完全不可达 | P0 | **已闭环（第 20 轮）**：新增发现页、方法页、选择/继续页、答题分发页、大五答题页、大五报告页、统一报告列表，共 9 个视图 + 2 个 store + 1 个 API 模块；后端补 `BigFivePlatformIT` 10 条端到端用例 |
| A67 | **AI 解读过长且抽象**（用户点名的三个核心问题之一） | `typeme-ai-prompt-v2.txt` 要求 800–1200 字、2–6 个 section；`ReportAnalysisValidator` 的 `schemaVersion` 固定 `"1"`、逐条校验 9 条 | 用户要的"围绕我关心的点、短、给一条可试的动作"拿不到；改输出必须同时改提示词、输入投影、输出 schema、Java 校验、前端模型与渲染，半途而废会让**旧分析记录读不出来** | P1 | **未开始（有意）**：本轮把预算放在多量表平台化上；这一项需要一次完整贯通，不能只在提示词上改一句话 |
| A68 | `frontend/package.json` 的 description 仍写 32 题旧流程 | 见该文件 | 无用户可见影响，但会误导后来的人 | P3 | **已闭环（第 20 轮）**：改成"多量表测评平台：十六型参考测评（服务端计分）与大五倾向测评（IPIP-50）" |
| A69 | **方法说明页换 slug 不重新取数据**：`/instruments/a/method` → `/instruments/b/method` 是同一个组件实例，只在 `onMounted` 取数据 | 真实浏览器验收实测："十六型方法页给出四个维度"数出了 **5 个**（页面继续显示上一项的维度与版本）。代码复核与单元测试都没抓到，因为页面**看起来有内容** | 用户会照着一项测评的口径去理解另一项（大五被当成"小一号的十六型"）；两项的维度数量不同（4 vs 5），最容易被误判成"内容本身有问题" | P1 | **已闭环（第 20 轮）**：改用 `watch(slug)` 重新载入，并加**请求令牌**防"后发先至"（先发的慢请求后回来会把 B 写成 A 的数据）；`instrumentMethodView.spec.ts` 用**站内跳转**钉住 —— 去掉 watcher 后两条测试精确变红 |
| A70 | 方法说明页**没说"结论长什么样"**，也没说分数怎么读 | 判据来自浏览器验收：大五方法页当时只有维度说明，没有一句说它**没有类型码、没有总分** | 只写"看哪几个方面"会让用户以为两项测评的结论是同一种东西；大五用户会去找自己的"四个字母"，十六型用户会去找总分 | P2 | **已闭环（第 20 轮）**：按 `hasTypeCode` 分流一段说明（大五：没有类型码也没有总分、某方面作答不够只影响那一个方面、分数说的是离中间值多远；十六型：四维组合、略偏说"更接近"、平分不给字母）；页面测试与浏览器验收各钉一次。顺带把方法页的「开始这项测评」改成带上当前 slug（原来是不带参数的死链） |
| A71 | **API 层（`api/platformV3.ts`）在此之前一条测试都没有**，替身夹具与真实 DTO 长期对不上 | 补测试时立刻撞出：`ReportDetailView` 要 `attemptRevision`，大五维度要 `direction`/`question`/`sideLabel`/`dailySigns`，版本行要 `revision`/`reportContentVersion`/`sha256` —— 而"按看起来合理"编的夹具全都没有 | 任何字段名对不上都会让页面白屏或空态，而失败信息是"服务器返回了看不懂的内容"，指不到具体字段 | P2 | **部分闭环（第 20 轮）**：新增 `api/platformV3.spec.ts` 11 条（逐字断言请求路径、slug 转义、目录/答题/报告的解析、v1 无外壳报告仍可读、中点取服务端值）；判别力已验证（`BASE` 改回 `/api/v3/platform` → 路径测试精确变红）。**其余 API 模块（`v3.ts` 等）仍未覆盖** |


## 第 28 轮新增（真实 MySQL 授权后的方言复盘，2026-09-21）

| ID | 问题 | 证据 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A72 | **`V10__illustration_asset.sql` 在任何 MySQL 上都无法应用**：列名 `release` 是 MySQL 8 保留字（`RELEASE SAVEPOINT`），裸写进 DDL 直接语法错；而同一份迁移又用了 MySQL 8 专有的 `COLLATE utf8mb4_0900_as_cs`，5.7 也不认 | 真实 MySQL **8.4.0** 上跑方言 IT：`Migration V10__illustration_asset.sql failed / Error Code 1064 ... near 'release    VARCHAR(32)  NOT NULL,'`。`typeme_dev` 停在 V8、全机无 `illustration_asset` 表 → V10 从未成功应用过。H2 的 MySQL 模式不把 `release` 当保留字，所以排除真实 IT 的 400 项全绿照不出来 | **任何尚未应用 V10 的库（含当前 `typeme_dev`）下次启动时 Flyway 直接失败，整个后端起不来** | P0 | **已闭环（第 28 轮）**：列名改 `release_tag`，同步 `IllustrationAssetService`（SELECT/UPDATE/INSERT/取值）、生成器 `scripts/gen-image-publish.mjs`（emit-sql 输出与 V10 **逐字一致**，diff 为空）、方案文档 DDL、`IllustrationAssetIT` 列名断言；JSON 对外字段仍叫 `release`。因为该迁移不可能在任何 MySQL 上成功过，**原地修正**而非追加 V11（V11 永远轮不到执行）。验证：真实 MySQL 8.4 上三类 IT **8/8、0 跳过**；判别力已验证（改回 `release` → `ConcurrencyMySqlIT` 红 3 条） |
| A73 | **`ConcurrencyMySqlIT` 把“迁移失败”当成“环境缺失”整类跳过**，且失败时不清理临时库 | 修复前原文 `Tests run: 3, ... Skipped: 3` —— V10 的 1064 被吞成“跳过”，报告上看不出异常；机器上留下 `typeme_concurrency_5ecd0b6e9cbf8`、`typeme_concurrency_5ee552215ac8c` 两个库 | **正是它藏住了 A72**；失败退出路径不跑 shutdown hook，临时库每失败一次多一个 | P1 | **已闭环（第 28 轮）**：只有 `canConnect()` 为假才算环境缺失（跳过）；连上后建库/迁移失败**抛 `IllegalStateException` 变红**；`createDatabase()` 后立刻登记清理钩子，并在 `catch` 里再 `dropDatabaseQuietly()`。判别力：V10 改回 `release` → 从“跳过 3 条”变成 **3 errors / 0 skipped**，且失败后无残留库 |

## 第 30 轮新增（阈值方案 B：v4，2026-09-22）

| ID | 问题 | 证据 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A76 | **“浏览器验收拿的是过期/被污染的夹具”这条风险没有护栏**：报告夹具由 `JungReportCopyBaselineTest#writeBrowserFixtures` 在测试进程里写到磁盘，而“用变异后的源码跑一次测试”会顺手把夹具覆盖成变异版（且不重算 `sha256`），随后不再重生成的浏览器验收会**拿着过期数据**通过 | 做分母变异验证时踩到：临时把包内 `boundaryDenominator` 改成 10 跑一次 `JungReportCopyBaselineTest` → 同一轮把夹具写成 `denominator=10`；若只跑浏览器验收不重跑测试，页面上“每 5 题”永远验不到 | 验收证据可能不是在验当次源码 | P2 | **已闭环（第 30 轮）**：`scripts/browser-verify-score-v4.py` 启动时对每份夹具**自报的** `scoringVersion` / `reportContentVersion` 做断言（期望值写死在脚本里），不符即当场红并提示先重跑生成测试。判别力已验证：把 `v4-boundary.json` 的 `boundaryDenominator` 改成 10 → 首条即红。**残余风险写入 REPORT**：夹具仍由测试写盘，脚本只能检版本字段，不能检分母 |
| A77 | **前端 `thresholds.spec.ts` “当前包声明的政策”可能被写成自比**：`expectedV4Boundary` 是测试自己重算的同一条式子 | 变异验证：把测试自己的契约表从 `/5` 改成 `/10` → **精确 4 条红**（逐点表、等号格、与开发方案对比两条），说明这四条比的是“包声明值 ↔ 写死的期望表”，不是同一个表达式自比 | “测试里重算公式”很容易写成自比（两边一起改就永远绿）| P3 | **仅记录（已核实不是自比）**：`V4` 是从 `typeme-jung48-zh-v4.json` 读的真实包 |
| A78 | **v4 的“多出多少道补充题”只能给公式，给不了数量** | `T(9)` 1→3、`T(12)` 2→4、`T(16)` 3→6，但实际多出多少取决于真实答卷的 `\|S\|` 分布；仓库里**没有**真实答卷样本 | 任何“`TENTATIVE` 会多 X%”“平均多答 Y 题”的说法都没有依据；触发集合扩大本身是有代价的产品选择 | P3 | **仅记录（证据缺口）**：需真人试测；本轮**不宣称**提高测量准确性，也不由公式反推效果 |
| A79 | **契约 §10.1 的夹具编号与生成器脱节**：生成器 `scripts/gen-jung-fixtures.mjs` 新增了 `CASE-22..25`（v4 阈值格），而契约里 `CASE-22` 已占用于“`TIED` 报告”这个**报告层**场景；两处指的是不同东西，而契约那张表还是“夹具必测清单” | 对比 `git show HEAD:…/01-新测契约-v1.md`（只有 CASE-01..22）与夹具实际 25 例；生成器里 `CASE-22..25` 的 note 与契约 CASE-22 的 note 完全不同 | 同一编号在两份文档里指两件事，后续引用会混；且新阈值格在契约里**根本没登记** | P3 | **已闭环（第 30 轮）**：契约 §10.1 补登 `CASE-22..25`（注明是 v4 新增的等号/越出/负向/合并题集格），原“`TIED` 报告”那条改为 `CASE-26` 并说明它不属 §10 夹具条目；另把该表里裸露的 `\|S\|` 统一转义为 `\|S\|`（否则表格被拆列） |

## 第 31 轮新增（真实全栈验收：真 jar + 真 MySQL，2026-09-22）

| ID | 问题 | 证据 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A80 | **“报告接口真的会下发阈值政策字段”这件事只有 mock 覆盖**：`scripts/browser-verify-score-v4.py` 把 `/api/**` 全部 mock，它只能回答“前端拿到这份 JSON 会怎么渲染”；Java 侧 `SubmitReportIT` / `JungReportSchemaTest` 只断言状态码与包绑定。整条链条里**没有任何一层**看过真实 `GET /api/v3/reports/{id}` 的 `methodology` | 第 30 轮的覆盖表里，“页面渲染”那一格的输入是脚本手写的 JSON；真实响应形状其实是 `{attemptId, attemptRevision, report:{…,report:{methodology:…}}, selfReflection}`（比假设多一层嵌套），我第一版断言就写错了路径而全部落空 | 后端一旦漏发 `boundaryNumerator`/`boundaryDenominator`/`policyVersion`，前端 `readScoringPolicy` 会静默退回缺省值 `2/10`，报告页显示“每 10 题…”——**三层测试全绿，用户看到的是错的话** | P1 | **已闭环（第 31 轮）**：新增 `work/r31_e2e.py`（API 级 16/16）与 `scripts/browser-verify-real-stack-v4.py`（真实浏览器 19/19），两者都跑在真实 jar + 真实 MySQL 隔离库上；判别力做到**部署产物层**：分母 5→10 重打包重启后，API 级精确红 4 条（`报告状态` 由 TENTATIVE 翻成 REFERENCE）、浏览器级精确红 5 条；内容源还原后两条各自复跑回全绿 |
| A81 | **注册 IP 限流会把“重复跑同一条验收”挡成假红**：注册按来源 IP 固定窗口限流，而验收脚本本身就要注册新账号 | 首次跑真实浏览器验收卡在注册页 40s；直接探接口得 `429 RATE_LIMITED, retryAfterSeconds=2370`；同一条脚本连跑第三次才撞上 | 验收红的原因与代码无关，容易被误判成回归；也说明“跑一次就存档”的验收模式在限流存在时不可重复 | P3 | **已处置（第 31 轮，脚本层）**：`browser-verify-real-stack-v4.py` 启动时**只删隔离库里 `register:%` 这一个桶**（`clear_register_rate_limit()`），不动别的库、不动登录/找回计数，并把理由写进脚本注释。**限流本身是正确行为，未改。** 残余：正式环境验收无法这样清理，仍受窗口限制 |
| A82 | **`/assess` 已是“选择页”，不再是“进去就建一份草稿”**：多量表之后路由注释已写明改因，但两条历史验收脚本（`browser-verify-jung-flow.py`、第 30 轮的 mock 脚本）仍按“跳 `/#/assess` 就建测评”写 | 第 31 轮真实浏览器验收直接跳 `/#/assess` 后 URL 原地不动、0 题可答；真实路径是在选择页点 `[data-start='jung48']` | 用旧假设写的端到端脚本会完全跑不动（看上去像产品坏了），而单元/组件层测试看不出这个问题 | P3 | **已记录（第 31 轮）**：新脚本按真实路径走；**未改历史脚本**（属已归档证据，改它们会串改历史）。后续新增端到端脚本应复用选择页入口 |
| A83 | **WSL→Windows 互操作不透传自定义环境变量，导致“我以为指向隔离库、实际写到了 `typeme_dev`”** | 用 `export TYPEME_DB_URL=…typeme_r31_e2e && java -jar …` 启动后，日志是 `Database: jdbc:mysql://127.0.0.1:3306/typeme_dev` → `Successfully applied 2 migrations to schema typeme_dev, now at version v10` | 这是**非预期写入**：本意是隔离验证，结果对现有开发库执行了 V9/V10 迁移并登记了 v3/v4。若换成生产库，影响会更重 | P2 | **已记录并核对（第 31 轮）**：在用户“全部授权”范围内，且结果与“待执行的部署步骤”一致；已核对 `assessment_attempt` 9→9、`assessment_report` 5→5 未变。处置：后续一律用带 `set TYPEME_DB_URL=…` 的 `.cmd` 启动（变量在 Windows 侧设置），并在 REPORT 里显式记录 |

## 第 32 轮新增（PR #17 的 Codex 评审，2026-09-22）

| ID | 问题 | 证据 | 影响 | 级别 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A84 | **「重新生成失败」会把上一次成功的正文从界面上抹掉**：`requeueForRetry` 允许从 `SUCCEEDED` 重新入队，且全仓库只有 `markSucceeded` 会写 `response_json` —— 重新入队与 `markFailed`/`markUnknown` **都不清它**。而 `AiAnalysisPanel.vue` 把失败卡与结果卡写成同一条 `v-else-if` 链，`status=FAILED` 时整片正文被挡掉 | 评审指出；代码核对：`AnalysisJobRepository.java:229`（`status IN ('FAILED','UNKNOWN','SUCCEEDED')`）、`:183`（唯一写 `response_json` 处）、`markFailed`/`markUnknown` 均未触碰该列；`AiAnalysisPanel.vue` 原 `v-else-if="failed"` / `v-else-if="succeeded"` | 点一次「再生成一次」失败，用户此前可读的分析就从页面消失（数据其实还在库里）。与 `AiAnalysisPanel.vue` 里「失败不消耗一次分析」的既有文案也自相矛盾 | P1 | **已闭环（第 32 轮）**：模板拆成两条独立 `v-if`，新增 `staleResult`/`showResult`，失败时把上一次正文继续显示并加 `[data-ai-stale]` 说明「这份是上一次成功生成的内容，本次未替换」；新增 2 个前端用例（含反向：从未成功过时不渲染结果区）。判别力：把 `showResult` 退回 `succeeded` → 精确红 1 条（`expected false to be true`），还原后 15/15 |
| A85 | **`V10` 的「不存在已应用过旧版 V10 的库」这一结论，此前只有推理、没有证据**：README/迁移注释/`REPORT.md` 都把「原地改 V10 而不是追加 V11」建立在「没有任何库成功执行过旧版 V10」之上，但从未在真实 MySQL 上验证过 | 评审建议改为增量迁移。补做证据（见 `docs/optimization/verification/2026-09-22-pr17-review/`）：① 原版 V10 在真实 MySQL 8.4 上 **`ERROR 1064`，连表都没建出来**（`mysql8-original-v10-rejected.txt`）；② 全机三个 TypeMe 库的 `flyway_schema_history` 里 `version>=9` 只有 `typeme_dev` 的 9/10 两条，其余全空（`mysql-flyway-history-and-column.txt`）；③ 全库只有 `typeme_dev.illustration_asset` 存在，列名已是 `release_tag`，没有 `release`；④ 两个 H2 文件库迁移停在 V8，且 0 处 `illustration_asset` 字样；⑤ 旧版 DDL 在 H2 MySQL 模式下**可以**成功执行（`h2-old-v10-accepted-by-h2.txt`），所以「H2 能过、MySQL 不能」这句话成立 | 若真有库应用过旧版 V10，原地改会先撞 Flyway checksum 不匹配，再撞「表里只有 `release`、代码查 `release_tag`」。现在这条结论有可复查的证据支撑 | P3 | **已闭环（第 32 轮，证据补齐）**：结论不变（**不追加 V11**），但把「不存在这种库」从推理升级为可复现证据；H2 文件库与 MySQL 全库清单已存档 |

## 第 29 轮新增（A58 放开目录 GET 时暴露的两个通用缺陷，2026-09-21）

| ID | 问题 | 证据 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|---|
| A74 | **jung / platform / ai 三个异常 advice 都拦不住共用能力抛出的 `com.typeme.common.ApiException`**，429 会变成裸 `ServletException`（MVC 兜底后是 500），错误体也不是契约 §7.1 的形状 | 首次跑新用例：`RateLimitIT#anonymousCatalogReadsAreRateLimited` 报 `jakarta.servlet.ServletException: Request processing failed: com.typeme.common.ApiException: 访问过于频繁，请稍后再试。`，状态不是 429。原因：`RateLimitService` 在 `com.typeme.security`、抛的是账号模块异常类型，而 `GlobalExceptionHandler` 的 `basePackages` 只到 `com.typeme.account`，`JungExceptionHandler`/`AnalysisExceptionHandler` 只认自己的异常类型。**A58 之前没有任何 jung/platform/ai 控制器会抛这个类型**，所以缺口一直没被触发 | 任何被限流/校验拒绝的非账号端请求都会变成 500，前端拿不到 `RATE_LIMITED` 与 `retryAfterSeconds`，只能显示“服务端错误”并让用户盲试 | P2 | **已闭环（第 29 轮）**：`JungExceptionHandler` 与 `AnalysisExceptionHandler` 各加一条 `@ExceptionHandler(com.typeme.common.ApiException.class)` 显式映射（**只加这一个类型、不扩 `basePackages`**，避免两个 advice 争 `Exception.class` 的注册顺序）。**AI 那条没有可触发路径**：`RateLimitService.checkAiCreate` 全仓无调用方，属对称补齐，不算端到端验证（已写进 REPORT） |
| A75 | **`CurrentUser.requireUserId()` 把匿名访客当成已登录用户**：Spring 的 `AnonymousAuthenticationToken.isAuthenticated()` 是 **true**、`getName()` 返回字面量 `"anonymousUser"`，于是“已登录”判定成立 | 第二次跑新用例：`anonymousCatalogReadsAreRateLimited` 得到 **200** —— 匿名限流一次都没发生（`JungController` 以为“已登录”而跳过）。判别力：只撤掉 `CurrentUser` 的两处匿名判定 → 该条精确红（`200 != 429`），其余 14 条绿 | 所有“靠 `isAuthenticated()` 判断是否登录”的分支对匿名请求都会当成已登录：按账号维度限流对匿名失效、匿名路径被跳过、埋点/审计会把匿名算成用户 | P2 | **已闭环（第 29 轮）**：`com.typeme.jung.api.CurrentUser` 新增 `isAuthenticated()`（排除匿名主体），`requireUserId()` 同样排除；`com.typeme.ai.controller.AiCurrentUser.requireUserId()` 做同样排除。**账号模块自己的 `CurrentUser.require()` 本来就排除了匿名，未动。** 同类“只看 `isAuthenticated()`”的写法是否还有别处，本轮未做全仓扫描 |

## 历史缺口复核（线索 → 现状）

| 线索（历史文档） | 复核结论 | 状态 |
|---|---|---|
| 首页下半部分仍由旧内容包驱动（`大五`/`50 题`） | **已不成立**。`LandingView.vue` 整页口径唯一来自 `instrumentV3.facts`（`GET /api/v3/catalog/current`，离线退新测内置口径）；该文件里剩余的"大五"只出现在解释历史的注释中 | 已闭环 |
| 小字号对比度 3.35–3.62:1 | **仍成立**，见 A3 | 已闭环（第 2 轮） |
| AI 分析前端界面缺失 | **已不成立**：`api/v3Ai.ts` + `stores/aiAnalysisV3.ts` + `components/AiAnalysisPanel.vue` 已接入报告页，真实浏览器验证 31 项通过 | 已闭环（第 7 轮） |
| AI 管理员后台配置页 | **已不成立**：`api/v3Admin.ts` + `views/AdminView.vue` + `composables/useAdminProbe.ts` 已接入；普通账号看不到入口、直接访问得到权限说明。真实浏览器验证 31 项通过（分两段：重启后端引导管理员） | 已闭环（第 9 轮） |
| 报告对比视图缺失（`GET /reports/compare`） | **原表述不准确，已纠正**：客户端 `compareReports()` 与 `CompareResult` 早就写好了，缺的是**页面与路由**（见 A12） | 已闭环（第 8 轮） |
| 答题页离线 outbox | **仍成立**：断网时作答不进队列 | 仅记录（需产品决定是否要"离线可答"） |
| 320×720 首屏放不下答题操作区 / sticky 顶栏占 19% | **已复核并修复**：320×568 上首页/登录/注册/关于顶栏均为 88px（15.5%），最差的历史值出现在**报告页**（139px / 24.5%，三个导航入口都在时）。修法是窄屏隐藏品牌行副标题。注意：这个数字是 **320 宽**机型的最差情况，390 宽起是 69–91px | 已闭环（第 4 轮） |
| 独立分享卡片模块（`utils/shareCardV3.ts`） | 代码组织问题，`shareImage.ts` 已存在且被复用；无用户可见影响 | 仅记录 |

## 需用户决定的事项

| ID | 事项 | 事实 | 建议 |
|---|---|---|---|
| A4 | 注册页是否必须勾选免责声明/条款同意 | **用户 2026-09-17 已决定：实施**（勾选框 + 服务端校验 + 契约同步）。见 progress.md 第 6 轮 | 已闭环 |
| — | AI 分析前端界面是否本轮做 | **用户 2026-09-17 已决定：做**，范围限定"报告页生成分析 + 展示 + 失败重试"，不做管理员配置页 | 已闭环（第 7 轮） |
| — | AI 管理员后台配置页是否要做 | **用户 2026-09-17 已决定：做**。已完成（第 9 轮）：只做 AI 设置读写 + 只读账号概览；改角色/禁用按"不可逆操作需单独确认"暂不开放入口 | 已闭环（第 9 轮） |
| — | 后台是否开放"改角色 / 禁用账号" | 后端 `PUT /admin/users/{id}/role`、`POST /admin/users/{id}/disable` **早已实现且被测试覆盖**，但前端未接。禁用会撤销对方全部会话、把账号置为不可登录，属不可逆操作 | 待用户决定（本轮只做了只读概览） |
| — | 是否需要可审计的同意/设置变更台账 | 目前注册同意只进服务端日志、AI 设置变更只有 `updated_at/updated_by` 两列；要做完整审计需加表/加列（DDL，需授权） | 待用户决定 |
| — | 是否引入 Redis | **仓库已有明确否决结论**：开发提示词第 40 行把"不引入 Redis/MQ/向量库"写成硬边界；`docs/2026-09-16/implementation/README.md` §6.3 专门讨论过（Redis 在本机可用但未采用，理由是"限流与额度已用 MySQL 事务保证一致性，多一个状态源就多一类两边对不上"）。**用户 2026-09-17 明确选择：作为后续架构项保留**（不改本版代码） | 见 A23 的架构评估；本版不动代码 |
| — | 是否引入 LangChain4j | **同一硬边界**（"不引入独立 AI 框架"）。用户 2026-09-17 询问；核实后建议**不引入**，理由见 A24。若仍要推进，建议做限时 PoC 并保留回退 | 待用户决定（默认：不引入） |
| — | 登录会话表缺少过期清理 | 见 A22。用户 2026-09-17 选择"收进 backlog 并先不动" | 仅记录（已知且可接受） |
