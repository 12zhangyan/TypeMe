# ── 依赖与构建产物（不进仓库，按 README「打包与部署」重建）──────────
node_modules/
frontend/dist/
backend/target/
coverage/
.vite/
*.tsbuildinfo
__pycache__/
*.pyc

# ── 本地环境与运行期文件 ──────────────────────────────────────────
.env
.env.*
*.local

# ── 编辑器 / 系统 ────────────────────────────────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db

# ── 刻意不发布的内容 ─────────────────────────────────────────────
# 抓取的第三方页面与截图（版权 / 商标风险）、研究原始素材、工作备份。
# 见 README「目录」与 LICENSE 的许可说明。
_research/
_osint/
research/
.baseline-backup/
.work-backup/

# 《天资差异》扫描件全文转写与解读：受版权保护图书的完整转录，只在本地留档，
# 不进 git 历史（docs 里的引用链接对未克隆该目录的人会失效，这是刻意取舍）。
/output/

# 一次性 OCR / 核对脚本、临时中间产物与运行日志。
# 方法论 v2 §「验证」自述：这些脚本「在 work/ 下，不进仓库校验链」。
/work/
