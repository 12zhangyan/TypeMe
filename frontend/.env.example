# TypeMe 前端构建期配置示例。
#
# 用法：复制为 frontend/.env.local（已被 .gitignore 忽略）后按需取消注释。
# 本文件只放**公开**配置：不放 SecretId / SecretKey / Token。
# 图片上传用的密钥只存在于运维侧的环境变量，前端构建不需要它。

# ── 后端 API 基地址（留空 = 同源 /api）──────────────────────────────
# VITE_API_BASE=https://api.example.com

# ── 图片域名：已退场，不要在这里配置 ──────────────────────────────
# 2026-09-20 起，页面插画的地址由**运行期读数据库**决定：
#   GET /api/v3/platform/illustrations（表 illustration_asset；改地址用仅 ADMIN 的 PUT）
# 所以这里既没有 VITE_IMAGE_BASE_URL，构建产物里也不该出现任何图片域名
# （`node scripts/check-bundled-image-urls.mjs` 会检查这一点）。
# 改图 / 换地址的做法见 docs/2026-09-20/图片URL入库方案.md 与
# frontend/src/assets/illustrations/README.md。

# ── 构建期开关：置 1 时生产构建保留验收答卷种子，正式发布不要开 ──────
# VITE_ENABLE_SEED=
