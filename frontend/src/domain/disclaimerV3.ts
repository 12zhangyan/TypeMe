/**
 * 注册页的免责声明同意项（2026-09-17 新增）。
 *
 * 为什么要有它：账号一建立，作答内容就开始存到服务器上。产品要求里有这条
 * （`docs/任务拆解.md` L105「免责声明」），但历史实现里前端没有这一项、后端也不读
 * 任何 `disclaimer*` 字段 —— 脚本发的键被静默忽略
 * （`docs/2026-09-16/verification/acceptance-evidence.md` §9.1 有实测记录）。
 *
 * **定位**：这里解决的是"账号一建立就开始存作答内容，这件事必须先被告知并同意"。
 * 与报告页的「这不是心理诊断」是两件事，不互相替代。
 *
 * **不是法律文本**：`docs/需求文档.md` §8 与 `docs/商业化构想-v0.1-备查.md` §8.3
 * 都要求正式上线前有隐私政策/用户协议正文。本文件只负责"注册时确实取得了显式同意"
 * 这一环，文案本身援引 `/about` 里已经写实的限制说明，不在这里自造承诺。
 */

/** 同意项的字段名，必须与后端 `AccountService.DISCLAIMER_FIELD` 一致。 */
export const DISCLAIMER_FIELD = 'disclaimerAccepted'

/**
 * 同意文案的版本。
 *
 * 文案实质改变时递增；它会在注册请求里一起送出去，这样"用户当时同意的是哪一版"
 * 在服务端日志里可查，而不用事后猜。注意：它**不**参与后端校验
 * （后端只认 `disclaimerAccepted === true`），版本校验留给后续
 * "条款版本升级需重新同意"时再引入。
 */
export const DISCLAIMER_VERSION = 'typeme-disclaimer-v1'

/**
 * 勾选框文案的分段，供页面渲染**一个** `<label>`（整句可点，读屏一次读完）。
 * 页面与测试共用同一份，避免测试另外抄一份文案出来、改文案时两边不一致。
 */
export const DISCLAIMER_TEXT = {
  before: '我已阅读并理解本站的',
  limitLink: '测评定位与限制',
  middle: '与',
  dataLink: '作答数据怎么存',
  after: '：这是自我探索用的参考测评，不是心理诊断；作答内容会存在服务器上、归我这个账号。',
} as const
